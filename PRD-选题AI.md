# PRD · 选题 AI（模块三 / 业务环节 3）

> 文档状态：Implemented v0.4 ｜ 范围：单模块深度定义 ｜ 平台：Windows 本地桌面应用（无云端）
> 关联：可选消费「账号定位」（模块一）+「收藏热点」（模块二）；下游供「框架 AI」（模块四）。模型调用统一走 `技术规格-模型网关.md`。
> 平台约束：Windows 本地应用，数据本地存储，API Key 不出本机。
>
> **v0.3 重大修订（用户决策）**：
> 1. **入口收敛为两种**：收藏热点 / 手动输入（删除"空白手建"，手动主题并入手动输入）。
> 2. **AI 输入固定为**：`账号信息（<账号定位> XML）` + `热搜关键词`（来自收藏热点或手动输入）。两种入口统一收敛到此输入。
> 3. **输出 JSON 结构由 schema 决定，且随 schema 变而变（固定机制）**：schema 字段名即 JSON 的键；schema 改，输出随之改。
> 4. **schema 可配置且"修改即生效为新默认"**：默认 7 字段，用户在设置增删改后，最新 schema 持久化为全局默认，后续生成一律按最新 schema 出 JSON。
> 5. **全局字段 schema 一套**：所有账号共用，不做每账号覆盖。
> 6. **收藏进"选题库"，不打标签、平铺**。
> 7. **状态机放宽口径确认**：锁定=定稿基线，非下游硬门槛，关联即可串接。
>
> **v0.4 实现确认（2026-07-28）**：
> 1. 已实现手动主题与收藏热点带入两个入口；收藏入口会携带不可变快照引用。
> 2. 当前生成仅允许选择已锁定的账号定位，并固定记录其精确账号版本；每个批次可发起 1–5 次独立模型调用，单项失败不阻断其余结果。
> 3. 已实现全局 schema（默认 7 字段）、字段增删改排序、恢复默认；模型输出必须严格匹配当前 schema。
> 4. 已实现草稿卡片、编辑即新版本、锁定/解锁、加入/移出平铺选题库和删除。

---

## 1. 背景与 Why

选题是整个流水线的**决策层**——回答"写什么、写给谁、什么角度、核心观点"。用户提出的核心原则是：**不要强关联**。

正确模型是：

> **每个产物都是独立实体，通过"关联字段"软串接；流程可以顺下来，也可以从任意环节直接开始，只需填入对应的东西。不强耦合，靠关联把流程串起来。**

因此选题模块的 AI 输入被**固定收敛**为两样东西：
- **账号信息**（`<账号定位>` XML，可选，可空）
- **热搜关键词**（来自收藏热点自动带入，或用户手动输入）

无论入口是"从收藏热点"还是"手动输入"，最终都变成 `(账号信息, 热搜关键词)` 这一统一输入 → 选题 AI → 输出 JSON 列表。

---

## 2. 目标（Goals）

- **入口简洁**：只有两种启动方式（收藏热点 / 手动输入），不强关联。
- **输入固定**：AI 输入 = 账号信息 + 热搜关键词，两种入口收敛到同一输入。
- **结构由 schema 决定**：AI 输出 JSON 列表，字段 = 全局 schema 字段名；schema 改，输出随之改（固定机制）。
- **schema 可配置且即时持久化**：默认 7 字段，用户改后即成新默认。
- **解耦批量**：批量生成 = N 次独立调用，单任务失败隔离。
- **可读可干预**：卡片墙一排 5 个、最新置顶、完整呈现；可编辑/收藏/删除/锁定。
- **可串接**：通过关联字段（账号/热点）与上下游软连接。

---

## 3. 用户故事（User Stories）

- 作为**运营者**，我希望从收藏的热点一键发起选题，AI 自动拿账号定位 + 该热点关键词出选题。
- 作为**运营者**，我希望直接打一个关键词/主题就让 AI 出选题，不必先去收藏热点。
- 作为**运营者**，我希望选题字段是我配置出来的（如加"系列栏目"），AI 按我的字段出 JSON——且我改完字段后，以后都按新字段出。
- 作为**运营者**，我希望生成的选题以卡片完整展示、最新的在最上面，方便一眼挑；不合适的直接删，合适的收进选题库。

---

## 4. 功能需求

### 4.1 创建入口（两种，不强关联）

只有两种启动方式，均进入选题工作台：

- **A. 从收藏热点**：从选题库（或收藏列表）勾选热点 → 自动带入 `热搜关键词`（= 该热点标题/关键词），并写入 `relatedHotIds` 软引用。
- **B. 手动输入**：用户直接输入一段关键词/主题文本 → 作为 `热搜关键词`；`relatedHotIds` 可空。

> 两种入口都收敛为同一输入：`账号信息（可选）` + `热搜关键词`。不存在"空白手建"——手动输入即提供关键词，AI 据此生成。

**关联是软引用（可选）**：
- `accountIds: string[]`（可空，支持多账号关联）
- `relatedHotIds: string[]`（可空，仅在入口 A 时填充）
- 两者均可空——允许完全独立的选题存在。关联仅用于溯源与下游串联，**不构成生成前提**。

### 4.2 AI 输入与输出（固定机制）

**输入（固定为两要素）**
```
[账号信息]  = <账号定位>…</账号定位>   （可选，accountIds 为空时不带）
[热搜关键词] = 字符串（入口 A 自动带入 / 入口 B 手动输入）
```
提示词示例（伪代码）：
```
你是选题策划助手。以下是账号定位：
<账号定位> {账号名称：… 领域：… 写作风格：…} </账号定位>

请围绕以下热搜关键词生成选题："{热搜关键词}"

请严格按当前字段模板输出 JSON 数组，每个对象为一个选题，键名必须如下：
{选题主题, 切入角度, 目标读者, 核心观点, 情绪基调, 拟标题方向, 备注}
每个选题包含 1 个方向即可（批量由多次调用实现）。只输出 JSON。
```

**输出（JSON 列表，由 schema 决定）**
```json
[
  {
    "选题主题": "大模型迭代加速，普通人该如何自处",
    "切入角度": "从工具替代焦虑切入，而非纯技术解读",
    "目标读者": "对AI好奇但担心被替代的职场人",
    "核心观点": "AI不是来抢饭碗的，是来放大普通人的",
    "情绪基调": "共情+理性",
    "拟标题方向": "当大模型再次进化，普通人该慌吗？\n别怕AI抢活，怕的是你不会用它",
    "备注": "可结合一个真实职场案例"
  }
]
```

> **固定机制（核心不变量）**：输出 JSON 的键 = 全局 schema 的字段名。schema 增/删/改字段名 → 提示词随之改 → AI 输出结构随之改。系统解析字典键渲染卡片，**字段完全由 schema 决定，不写死**。

**格式约定（对全局 XML 包裹约定的细化，不冲突）**
- **生成产物（选题内容）**：用 **JSON**（列表/字典），字段可配置、易解析、卡片渲染友好。
- **下游消费（作为 LLM 上下文）**：仍遵循全局「XML 标签包裹」约定，把选题 JSON 序列化后包进 `<选题>…</选题>`，保证 prompt 层面结构一致。
- 即：账号定位 → XML 纯文本；选题（AI 产物）→ JSON 存储/渲染，下游消费时统一过 XML 包裹层。两层职责不同，不矛盾。

### 4.3 全局字段 Schema（默认 7 字段，可配置，修改即生效为新默认）

**默认字段（MVP 全部纯文本，全局一套）**

| 字段名 | 说明 | 必填 | 备注 |
|--------|------|------|------|
| 选题主题 | 一句话主题 | 必填 | |
| 切入角度 | 差异化视角（和别人写有什么不同） | 必填 | |
| 目标读者 | 账号受众里的哪一类人 | 必填 | |
| 核心观点 | 文章要立住的一个 thesis | 必填 | |
| 情绪基调 | 理性 / 共情 / 犀利 / 争议… | 必填 | |
| 拟标题方向 | 1–3 个候选 | 必填 | MVP 以纯文本多行表示（一行一个），结构化数组为 P1 扩展 |
| 备注 | 人工补充 | 可选 | |

**可配置 & 即时持久化**
- 用户在**设置 → 选题字段**中可：新增字段、删除字段、重命名字段、调整顺序。
- **修改后该 schema 立即持久化为全局默认**（写覆盖原默认），后续所有生成一律按最新 schema 出 JSON。
- 提供「恢复默认」按钮重置回上述 7 字段。
- **全局一套**：所有账号共用同一 schema，不做每账号覆盖（与你确认一致）。
- 单卡片支持**增加自定义字段**（键值对，一等公民，进同一 JSON 对象）；卡片级自定义字段不影响全局 schema。

**系统内部数据模型**
```
Topic {
  id: string;
  accountIds: string[];          // 关联账号（软引用，可空）
  relatedHotIds: string[];       // 关联热点（软引用，入口 A 时填，可空）
  seedKeyword: string;           // 热搜关键词/主题种子（AI 输入溯源，重生成用）
  fields: { [key:string]: string };  // 选题字段（来自 schema + 卡片级自定义），纯文本
  status: "draft" | "locked";
  version: number;
  createdAt: string;
  updatedAt: string;
}
```

### 4.4 批量生成机制（N 次独立调用 = 解耦）

**生成配置**
- 账号池 `accountIds`（可空）
- 关键词池 `keywordPool`（来自收藏热点自动带入的关键词 + 手动输入的关键词，可空）
- 生成数量 `count = N`（用户设定，代表发起的 AI 调用次数）

**执行（解耦核心）**
- 系统发起 **N 次独立的 AI 调用**（可并行），**每次调用产出 1 个选题 JSON 对象 → 渲染为 1 张卡片**。
- 每次调用按**轮询分配**从账号池/关键词池中分别取一个作为该次上下文（池为空则不带对应上下文）。
- 单任务失败（某次调用报错/超时）不影响其他任务，失败项标记"生成失败"可单独重试。

**分配策略（默认轮询，笛卡尔可选）**
- **轮询分配（默认）**：第 i 次调用取 `accountIds[i % len]` 与 `keywordPool[i % len]`。总调用数 = N，可控。适合"我要 N 个多样化选题"。
- **笛卡尔积（可选开关）**：对 `每个账号 × 每个关键词` 组合各生成一次（每组合数量可配）。总调用数 = 账号数 × 关键词数 × 每组合数量，会快速膨胀。适合"我要穷举覆盖所有组合"。
- MVP 默认 **轮询**；笛卡尔作为"穷举模式"开关提供。

**为何这样解耦**：① 多账号/多关键词/多选题天然通过"任务并列"实现，无需复杂组合逻辑；② 单次失败隔离；③ 支持并行提速；④ 与全局"关联而非耦合"原则一致。

### 4.5 呈现与操作（卡片墙）

- **卡片墙**：一排 **5 个**卡片，纵向排列；**最新生成的排在最上面**（顶部）。
- 每张卡片**完整呈现**所有字段（含自定义字段），方便查看。
- 卡片操作：查看 / 编辑 / 增自定义字段 / **收藏**（进"我的选题库"）/ **删除** / 锁定。
- 删除 = 从库移除（草稿/锁定均可删，删前确认）。

### 4.6 选题库（收藏即入，不打标签、平铺）

- 卡片点「收藏」→ 加入本地**选题库**（`topic_library`），供框架 AI（模块四）后续关联消费。
- **不打标签、平铺**：选题库是单一列表，所有收藏的选题平铺展示，不做分类标签（与你确认一致）。
- 选题库中的选题可再次编辑、锁定、删除，或作为框架 AI 的关联源。

### 4.7 人机协同（HITL）与状态机（放宽口径已确认）

- **草稿态**：生成后即草稿，所有字段可编辑，可增删自定义字段。
- **锁定**：用户点"锁定"→ `locked`（代表定稿基线）；但**锁定不再是下游消费的硬门槛**（已确认放宽）——下游（框架 AI）可通过关联消费草稿态选题。
- **换模型/重生成**：生成新版本，旧版本进历史可回滚，不丢手动编辑；重生成时复用 `seedKeyword` + 当前 `accountIds`，按**最新 schema** 出 JSON。
- **关联可后补**：即使生成时未关联账号/热点，事后可在卡片上补关联。

**与上下游的关联（非耦合）**
```
[账号定位]──┐  (可选关联)
[收藏热点]──┼──► 选题（独立实体，可无关联独立存在）
[手动输入]──┘       │ 关联(accountIds/relatedHotIds)
                    ▼
              框架 AI（模块四，按关联消费，草稿/锁定均可）
```
- 任一上游缺失都不阻断选题；选题缺失也不阻断下游手建。整条链路靠"关联字段"松接。

---

## 5. 前置依赖

- **模型网关**（见 `技术规格-模型网关.md`）：选题 AI 经 `gateway.chat()` 调用（支持 json_mode）。
- **账号定位模块**（模块一，已封板）：可选上下文 `<账号定位>`。
- **热点模块**（模块二，已封板）：可选关联 `relatedHotIds`（仅入口 A 使用，非必须）。
- **选题字段 schema 配置**：设置中维护，生成前需有可用 schema（默认 7 字段，可改）。

---

## 6. 成功指标（Success Metrics）

| 指标 | 类型 | 目标 |
|------|------|------|
| 选题生成成功率（N 次调用中成功比例） | 健康 | ≥ 95% |
| 单卡片字段解析成功率（JSON 合规，键匹配 schema） | 健康 | ≥ 98% |
| 用户收藏率（生成→收藏进库） | 驱动 | 观察选题质量 |
| 独立选题占比（无关联账号/热点的选题） | 洞察 | 验证松耦合价值 |
| 生成耗时（P95，N=5 并行） | 健康 | < 30s |
| 批量隔离有效性（单失败不影响其他） | 健康 | 100% |
| schema 配置留存率（用户改过 schema 后按新 schema 出 JSON） | 健康 | 100% |

---

## 7. 风险与缓解

| 风险 | 影响 | 缓解 |
|------|------|------|
| AI 输出 JSON 不合 schema / 解析失败 | 卡片渲染崩 | 网关 json_mode + 解析兜底；失败卡片标记"格式异常"可手动编辑 |
| schema 修改后下游消费错位 | 框架 AI 拿不到预期字段 | schema 为下游约定基线；关联时携带完整 JSON，缺字段下游容错 |
| 松耦合致溯源链断 | 修改不可追溯 | 关联为可选但鼓励填；卡片上显式展示关联实体 |
| 批量 N 次调用成本/限流 | 成本、限流 | 并发上限可配；单任务失败隔离；笛卡尔模式提示成本 |
| 锁定非硬门槛致下游用草稿 | 质量波动 | 下游消费时提示"该选题为草稿态"，用户知情 |

---

## 8. 验收标准（Acceptance Criteria）

1. 用户可从**两种入口**（收藏热点 / 手动输入）创建选题，关联字段均可空；两种入口都收敛为「账号信息 + 热搜关键词」输入。
2. 设定数量 N 后，系统发起 **N 次独立调用**，产出 N 张卡片；单任务失败不影响其他。
3. AI 输出 **JSON 列表**，键名 = 全局 schema 字段名；**schema 改，输出随之改**（固定机制验证）。
4. 默认 7 字段；用户在设置增删改字段后，**最新 schema 持久化为全局默认**，后续生成按新 schema 出 JSON。
5. 卡片墙**一排 5 个**、最新生成置顶、完整呈现字段。
6. 卡片支持 编辑 / 增自定义字段 / **收藏进选题库（不打标签平铺）** / **删除** / 锁定。
7. 下游（框架 AI）可经关联消费草稿态或锁定态选题（锁定非硬门槛，已确认）。

---

## 9. 开放问题（剩余少量，不阻塞 MVP）

1. **笛卡尔模式是否 MVP 就做**？还是先只做轮询，笛卡尔留 P1？（建议 MVP 仅轮询）
2. **拟标题方向的结构化**：MVP 用纯文本多行，是否需要 P1 升级为数组类型（影响 schema 类型系统）？
3. **选题库容量/检索**：平铺库若积累几百条，是否需要基础搜索/排序（按账号/时间）？（建议 MVP 仅时间倒序）

---

*下一步*：选题模块基本封板 → 模块四：框架 AI（消费关联选题 + 账号 + 素材，搭骨架）。
