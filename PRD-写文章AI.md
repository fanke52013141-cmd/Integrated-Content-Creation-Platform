# PRD · 写文章 AI（模块五 / 业务环节 6）

> 文档状态：Draft v0.2 ｜ 范围：单模块深度定义 ｜ 平台：Windows 本地桌面应用（无云端）
> 关联：上游消费「框架」（模块四，泛型解析 section）+「账号定位」（模块一，可选）+「素材」（模块六，可选）；下游供「评审 AI」（模块八，P1）消费成稿，最终经「排版/配图/导出」（P1）产出本地文件。模型调用统一走 `技术规格-模型网关.md`。
> 平台约束：Windows 本地应用，数据本地存储，API Key 不出本机。
>
> **v0.1 关键定位（用户本轮明确）**：
> **写文章 AI 不是"一次性生成"，而是「写 + 改」双职责**——既把 `<框架>` 扩写成完整成稿，又基于反馈/指令修改成稿。改稿是一等公民，不是附录。
>
> **v0.2 开放问题全部拍板（本轮）**：
> 1. **成稿格式 = Markdown**（下游排版/导出易消费）。
> 2. **编辑器 = 所见即所得富文本**（Markdown 为底，用户看渲染后富文本，可切源码）。
> 3. **目标字数不约束**：不在接口传字数硬参数，由提示词软约束（如"篇幅适中"），尊重用户"约束也没用"的判断。
> 4. **评审契约 = 现在定**（已写 §4.3，供 P1 评审 AI 对齐）。
> 5. **改稿也支持 count 候选对比**（同写模式，revise 模式同样可生成多个改稿版本对比）。

---

## 1. 背景与 Why

写文章 AI 是整个流水线的**内容生产核心**——把"框架（骨架）"扩写成"完整成稿（写）"，并基于反馈/指令"修改成稿（改）"。

用户明确：**写文章 AI 不仅写，还改。** 这把"单次生成赌质量"升级为"生成 + 迭代"的闭环：
- **写**：把框架的每个 section（标题/开头/论点×N/结尾）扩写成连贯段落，贴合账号定位的风格/IP/情绪基调。
- **改**：基于两类输入迭代成稿——① 用户自由指令（"开头改犀利些"）；② 评审意见（评审 AI 输出，P1 提供）。

边界（延续）：框架是骨架，写文章是成稿。框架不写成稿，写文章不回退去改框架结构（除非用户显式要求"回到框架对齐"）。

松耦合延续：输入含 `<框架>`（必填，泛型解析）+ `<账号定位>`（可选）+ `<素材>`（可选）。无素材模式继续有效；素材仅以网页 Summary 或手工文字摘要注入。

**依赖处理（已决策）**：评审 AI 是 P1，但改稿需消费其输出。因此本 PRD **先行定义「评审意见」XML 契约**（§4.3），评审 AI（P1）实现时对齐，避免改稿接口返工。

---

## 2. 目标（Goals）

- **写成稿**：基于框架逐 section 扩写，贴合账号风格/IP，输出连贯可读文章。
- **改稿一等公民**：支持「用户指令改」「评审意见改」「换模型重生成」三类触发，均产生新版本。
- **格式一致**：成稿用统一格式（默认建议 Markdown，见 §9 待决），下游排版/导出可直接消费。
- **版本管理**：每次写/改产生版本，可回滚、可对比，不丢手动编辑。
- **解耦可选**：素材可选，无素材也能写；账号定位可选，不带则仅用 LLM 通用能力。

---

## 3. 用户故事（User Stories）

- 作为**运营者**，我希望选中一个框架，AI 直接扩写成完整文章，不用我从骨架重写。
- 作为**运营者**，我希望 AI 写出来的文风跟账号定位一致（IP/风格/情绪基调不跑偏）。
- 作为**运营者**，我希望评审给完意见后，AI 能据此改稿，不用我手动把意见整合进文章。
- 作为**运营者**，我希望能手动说"把开头改犀利些""第二段太长缩短"，AI 就改对应处。
- 作为**运营者**，我希望每次改稿都留历史版本，改坏了能一键回退。
- 作为**运营者**，我希望素材模块做好前也能写文章（无素材模式），不被卡住。

---

## 4. 功能需求

### 4.1 创建入口（松耦合，两种）

- **A. 关联框架（推荐）**：从框架卡片墙选一个框架 → 自动带入 `<框架>`（泛型解析其 section）+ 该框架关联的 `accountIds` / `topicId` / `relatedHotIds`（溯源）。
- **B. 手动起**：用户粘贴/手填框架文本或仅主题 → `frameworkId` 可空，不关联框架。

> 两种入口都收敛为：`<框架>`（必填，除非纯手动主题）+ `<账号定位>`（可选）+ `<素材>`（可选）+ **模式（generate / revise）**。不强关联。

### 4.2 AI 输入（两种模式）

**写模式（generate）**
```
<账号定位> … </账号定位>     （可选，accountIds 为空时不带）
<框架> … </框架>             （必填；泛型解析所有 section：标题/开头/论点×N/结尾）
<素材> … </素材>             （可选，仅注入网页 Summary 或手工文字摘要；图片不作为正文事实依据）
```
> **篇幅约定**：**不传目标字数硬参数**。篇幅由提示词软约束（如"篇幅适中，约 1200–1800 字"），用户明确"约束也没用"，故不在接口层做硬性字数限制。

**改模式（revise）**
```
<账号定位> … </账号定位>     （可选）
<原稿> … </原稿>             （必填，当前成稿文本）
<修改指令> … </修改指令>     （必填，内容=用户指令 或 序列化的评审意见）
<框架> … </框架>             （可选；带则从框架对齐结构，不带则仅按指令改原稿）
```
> 篇幅同样由提示词软约束，不传字数硬参数。

提示词示意（写模式，伪代码）：
```
你是公众号文章写手。基于框架扩写成完整成稿（不要只给提纲）。

<账号定位> {写作风格：… IP人设：… 情绪基调：…} </账号定位>

<框架>
{标题/开头/论点一/二/三/结尾，各含核心句+支撑要点}
</框架>

要求：
- 逐 section 扩写：开头写钩子段；每个论点展开为核心段（核心句引领+支撑要点成文+案例/数据占位）；结尾收束+升华。
- 文风严格贴合 <账号定位> 的写作风格与 IP 人设。
- 输出 Markdown（# 标题、## 论点小标题、段落、列表、引用）。
- 不回退改框架结构，只基于框架写正文。
```

提示词示意（改模式，伪代码）：
```
你是公众号文章改稿编辑。基于修改指令修改原稿，做最小必要改动，保持原文风格与结构。

<原稿> … </原稿>
<修改指令> {用户指令 或 评审意见序列化} </修改指令>
（可选）<框架> … </框架>  （带则从框架对齐）

要求：
- 只改指令涉及处，不擅自重写无关段落。
- 若指令与框架冲突，以指令为准但提示用户。
- 输出完整新成稿（Markdown），非 diff。
```

### 4.3 评审意见契约（预留，供 P1 评审 AI 对齐）

即使评审 AI 在 P1，改稿需消费其输出，故**本 PRD 先行定义契约**，评审 AI 实现时对齐：
```
<评审意见>
评审角色：事实核查官
维度：事实 / 可读性 / 调性 / 合规 / 传播
问题列表：
  - 位置：第二段 ｜ 严重度：中 ｜ 问题：数据无来源 ｜ 建议：补充来源或弱化表述
  - 位置：标题 ｜ 严重度：低 ｜ 问题：钩子偏弱 ｜ 建议：增加悬念
总体建议：核心论点清晰，建议强化开头共鸣。
</评审意见>
```
改稿时可将**多个** `<评审意见>` 拼入 `<修改指令>`（多评审角色一轮汇总改）。

### 4.4 AI 输出（成稿，建议 Markdown）

```
# 当大模型再次进化，普通人该慌吗？

上周同事被"AI 替代"的传闻刷屏，我反问了一句——被替代的，真的是人吗？（开头段）

## AI 不是来抢饭碗，是来放大普通人的杠杆

历史规律表明，每一次生产力革命短期取代岗位、长期创造新职业……（论点一正文，支撑要点成文）
- 职场人用 AI 提效的样本（待素材补充）
- 反常识：最该怕的是不会用 AI 的人

## 焦虑的解药是"人机协作"的方法论
……（论点二正文）

## 普通人的护城河，是"定义问题"的能力
……（论点三正文）

工具会迭代，人的价值在"定义问题"。（结尾段，升华）
```

> **内部存储**：成稿以 Markdown 文本（`rawMarkdown`）存储。编辑视图为**所见即所得富文本编辑器**（Markdown 为底，用户看到渲染后的富文本，可切源码）；下游排版/导出基于 Markdown 转换。

### 4.5 写 vs 改：同一 AI 角色，两种模式

- **generate**：`<框架>` → 成稿。
- **revise**：`<原稿>` + `<修改指令>` → 新成稿。
- **换模型重生成**：复用同一输入，新版本（沿用全局 HITL 模式）。
- 两类模式共用同一模型网关调用、同一版本管理体系。

### 4.6 生成数量（候选，写/改均支持）

- **写模式**与**改模式**均支持一次生成 **count 个候选**（默认 1，可选 1–3），即 count 次独立 AI 调用。
- 写模式多候选用于对比不同写法/角度；改模式多候选用于对比"按不同指令/不同评审侧重"的改稿结果，用户择优保留。
- 框架比选题"重"，默认 1；多候选时多次独立调用、单任务失败隔离。

### 4.7 呈现与操作（成稿工作台）

- **成稿编辑器**：**所见即所得富文本**（Markdown 为底，实时渲染；可切源码编辑）；成稿为长文，编辑器内分段呈现。
- **版本历史**：列表展示每次写/改的版本，可回滚 / 对比（不丢手动编辑）。
- **操作**：查看 / 编辑 / 锁定 / 删除 / 关联评审（P1）/ 导出（本地 md/html，P1 排版前可先导出草稿）。
- count>1 时以**多版本/多候选**呈现（非卡片墙，成稿是长文，用版本列表更合适）。

### 4.8 人机协同（HITL）与状态机（放宽口径）

- **草稿态**：成稿可自由编辑（Markdown），可发起改稿。
- **锁定**：点"锁定"→ `locked`（定稿基线）；**非下游硬门槛**（排版/导出可用草稿态）。
- **改稿**：产生新版本（新 `rawMarkdown`），旧版本进历史可回滚，不丢手动编辑。
- **关联可后补**：生成时未关联框架/账号，事后可补 `frameworkId`/`accountIds`。

**与上下游的关联（非耦合）**
```
[框架]─────┐
[账号定位]─┼──► 写文章 AI ──► 成稿（generate / revise，版本管理）
[素材 P1]──┘        │
              <评审意见>(P1) ──► revise 改稿
                    │
                    ▼
              评审 AI(P1) / 排版(P1) / 导出
```

---

## 5. 前置依赖

- **模型网关**（见 `技术规格-模型网关.md`）：写文章 AI 经 `gateway.chat()` 调用。
- **框架模块**（模块四，已封板）：`<框架>` **必须泛型解析 section**（模板可编辑，section 名不固定）。
- **账号定位模块**（模块一，已封板）：可选 `<账号定位>` 上下文（文风/IP）。
- **素材模块**（模块六，已接入）：`<素材>` 为用户选中的网页 Summary 或手工文字摘要，附来源元数据；不注入网页全文，图片仅供视觉参考。
- **评审 AI**（P1，未做）：本 PRD §4.3 定义其输出契约，供其对齐。

---

## 6. 成功指标（Success Metrics）

| 指标 | 类型 | 目标 |
|------|------|------|
| 成稿生成成功率 | 健康 | ≥ 95% |
| 框架 section 覆盖完整率（每 section 都扩写） | 健康 | ≥ 98% |
| 文风契合度（人工/锁定率代理） | 驱动 | 观察 |
| 改稿采纳率（改完用户保留新版本） | 驱动 | 观察迭代价值 |
| 无素材模式可用性 | 健康 | 100% |
| 版本回滚成功率 | 健康 | 100% |
| 生成耗时（P95，单成稿） | 健康 | < 60s |

---

## 7. 风险与缓解

| 风险 | 影响 | 缓解 |
|------|------|------|
| 框架 section 泛型解析错位 | 漏写论点/结构乱 | 写文章 AI **严格泛型遍历** `<框架>` 所有 section；PRD 显式约束 |
| 成稿过长/过短 | 不符公众号篇幅 | 篇幅由提示词软约束（不传字数硬参数）；用户可手动调整 |
| 改稿偏离原意（过度改写） | 用户不认 | 改稿 prompt 约束"最小必要改动"；版本可回滚 |
| 评审意见格式未对齐 P1 | 改稿接不上 | §4.3 契约先行，评审 AI 实现时对齐 |
| Markdown 与公众号不兼容 | 导出失真 | 导出转换层（P1 排版负责）；MVP 先导出 md/html |
| 无素材时案例空洞 | 成稿缺实证 | 提示词标注"待素材补充"占位；P1 改善 |

---

## 8. 验收标准（Acceptance Criteria）

1. 用户可从**两种入口**（关联框架 / 手动）创建成稿，关联字段可空；收敛为 `<框架>`+`<账号定位>`+`<素材>`+模式。
2. **写模式**：按 `<框架>` **泛型解析**所有 section 扩写成稿，文风贴合 `<账号定位>`（若带）。
3. **改模式**：支持 **用户指令改** 与 **评审意见改**（§4.3 契约）两类触发，输出完整新成稿；**改稿也支持 count 候选对比**（同写模式）。
4. 成稿格式统一为 **Markdown**（已确认；下游排版/导出基于 Markdown 转换）。
5. **无素材模式**：`<素材>` 为空时正常写文章。
6. **写/改均支持 count 个候选**（默认 1），多次独立调用。
7. **版本历史**：每次写/改产生版本，可回滚、可对比，不丢手动编辑。
8. 下游（评审/排版/导出）可消费草稿态或锁定态成稿（锁定非硬门槛）。
9. 编辑器为**所见即所得富文本**（Markdown 为底，可切源码）。

---

## 9. 开放问题（旧 5 项均已本轮拍板，无新增阻塞项）

1. ~~成稿格式~~ → **Markdown**。（已决）
2. ~~改稿编辑器形态~~ → **所见即所得富文本**（Markdown 为底，可切源码）。（已决）
3. ~~目标字数~~ → **不约束**，篇幅由提示词软约束，不在接口传字数硬参数。（已决）
4. ~~评审意见契约~~ → **现在定**（已写 §4.3，供 P1 评审 AI 对齐）。（已决）
5. ~~改稿候选~~ → **改稿也支持 count 候选对比**（同写模式）。（已决）

> 遗留关联事项（非本模块）：P1 评审 AI（模块八）须按 §4.3 契约输出 `<评审意见>`，否则改稿的"评审意见改"触发接不上；素材模块（P1）接入后框架/写文章自动加 `<素材>`。

---

*下一步*：写文章模块封板（模块一~五 P0 主线全通）→ P1 模块：素材搜索（搜索拆解 AI + 素材浓缩 AI）/ 评审 AI（对齐 §4.3 契约）/ 配图三件套 / 排版。
