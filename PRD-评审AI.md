# PRD · 评审 AI（模块八 / 业务环节 7）

> 文档状态：Draft v0.2 ｜ 范围：单模块深度定义 ｜ 平台：Windows 本地桌面应用（无云端）
> 关联：上游消费「写文章」的成稿（模块五，Markdown）；输出严格对齐「写文章 PRD §4.3 评审意见契约」，供其 `revise` 改稿模式消费（即"评审意见改"触发）；模型调用统一走 `技术规格-模型网关.md`；可参考 `账号定位`（模块一）、`框架`（模块四）、`素材`（模块六）作为评审上下文。
> 平台约束：Windows 本地应用，数据本地存储，API Key 不出本机。
>
> **v0.1 → v0.2 关键修订（用户本轮决策）**：
> **1. 角色库完全用户自建，无默认**——不预置任何评审角色，用户自己创建角色、写 system prompt、独立运行；调用时"选文章 + 选多角色"发起评审。
> **2. 程序化提取 `<评审意见>` + 规则可自定义**——模型可能先吐一堆思考内容（CoT）再给 XML，系统用**可配置的提取规则**从原始返回中截出 `<评审意见>…</评审意见>` 中间内容，再解析。
> **3. 严重度仅 高/中/低**，不加"阻塞级"，由用户自行判断要改哪些。
> **4. 无应用预览**——点"应用评审改稿"直接触发写文章 `revise`，不弹预览。
> **5. 每角色可指定独立模型**——角色配置含 `modelId`，走模型网关按角色选模型。
>
> **继承的既有决策**：评审只跑一轮即终；输出契约按写文章 PRD §4.3。

---

## 1. 背景与 Why

写文章 AI 已能把 `<框架>` 扩写成完整成稿（模块五），且支持"评审意见改"。但成稿质量靠"作者自己觉得行"不可靠——公众号文章要过五关：事实站不站得住、读不读得懂、调性对不对味、踩不踩合规红线、传不传得开。单个人（或单个 AI 视角）很难同时把这几关都看透。

传统做法是一个人写完，自己读一遍就发了，或者丢到群里让人挑刺，零散、不可追溯、容易漏。本模块用**多个专业评审角色并行审同一篇稿**，把"挑刺"结构化、可溯源、可选择性采纳，再一键驱动改稿——让成稿在发布前过一遍"虚拟编委会"。

核心用户问题：**"我怎么确定这篇稿子发出去不会翻车 / 不够好？"**

业务目标：**把单点成稿升级为"多视角把关后的成稿"，并把评审意见和改稿无缝串起来。**

---

## 2. 目标（Goals）

- **多角色并行评审**：同一篇成稿，按用户勾选的 N 个评审角色，各出一份独立 `<评审意见>`。
- **角色完全用户自建**：不预置任何默认角色；用户创建角色、写 system prompt、可配模型与提取规则，独立运行。
- **契约严格对齐**：输出格式 = 写文章 PRD §4.3 定义的 `<评审意见>`，不偏差（否则改稿接口接不上）。
- **程序化提取**：模型返回先经**可配置提取规则**截出 `<评审意见>` 块，再解析；抗 CoT/噪声。
- **一轮即终**：评审输出后，用户采纳→触发一次改稿，流程结束；不做多轮循环、不弹改稿预览。
- **结构化采纳**：每条意见可独立"采纳 / 忽略"，用户可手动追加自己的意见；采纳的意见拼进改稿的 `<修改指令>`。
- **上下文感知**：评审可带入 `<账号定位>`（调性对齐）、`<框架>`（结构对齐）、`<素材>`（事实回溯来源）作为参考。

---

## 3. 术语

| 术语 | 含义 |
|------|------|
| 评审角色 | 一个独立的评审视角，由用户自建：含 名称 + system prompt + 可选模型 + 提取规则。 |
| 评审意见 | 单个角色对一篇成稿的输出，严格为 `<评审意见>` 块（角色/维度/问题列表/总体建议）。 |
| 评审任务 | 对一篇成稿发起的一次评审（含选中的角色集 + 产出的多份评审意见）。 |
| 维度 | 评审切入面（自由文本，由角色 prompt 定义，如 事实/可读性/调性/合规/传播）。 |
| 严重度 | 问题严重程度：高 / 中 / 低（用户自行判断是否需要改，**无阻塞级**）。 |
| 采纳 | 用户标记某条意见"要改"，之后拼进改稿指令。 |
| 提取规则 | 一条可配置的正则/匹配式，从模型原始返回中截出 `<评审意见>` 中间内容。 |

---

## 4. 用户故事

- 作为**运营者**，我希望多个专家同时审我的稿子（有人查事实、有人改文笔、有人看传播），而不是自己瞎看。
- 作为**运营者**，我希望每个评审角色是我自己建的、prompt 我自己写——比如我做的是财经号，我要加个"监管合规审查"角色，不想被固定角色限制。
- 作为**运营者**，我希望每个评审角色的意见是分开的，我能看清"谁说了什么"，而不是混成一坨。
- 作为**运营者**，我希望能勾掉不认同的意见（比如我觉得那个'钩子偏弱'没必要改），只把认同的送进改稿。
- 作为**运营者**，我希望能自己加一条意见（"第三段太啰嗦"），和 AI 的意见一起送改稿。
- 作为**运营者**，我希望点一下"应用评审改稿"，AI 就把采纳的意见都改进文章，不用我手动复制粘贴、也不用先弹个预览让我再确认。
- 作为**运营者**，我希望模型有时候先啰嗦一堆思考再给结果时，系统能自动把真正的 `<评审意见>` 抓出来，别把思考内容也当评审意见塞进改稿。

---

## 5. 需求详述

### 5.1 评审角色体系（用户自建，无默认）

**本模块不预置任何角色。** 角色库初始为空，由用户创建。每个角色是一份独立配置：

| 字段 | 说明 | 必填 |
|------|------|------|
| 名称 name | 如"事实核查官""合规审查员" | 是 |
| systemPrompt | 该角色的评审指令；**提示用户要求模型用 `<评审意见>` 包裹输出，并含 位置/严重度/问题/建议 四要素** | 是 |
| modelId | 该角色调用哪个模型（走模型网关）；为空则回退到全局默认评审模型 | 可选 |
| extractionRule | 从模型原始返回截出 `<评审意见>` 的规则（默认见 §5.3.1）；用户可改 | 可选（有默认） |
| order | 工作台展示顺序 | 可选 |

**角色创建引导（非预置角色）**：新建角色时提供一段**提示性模板**（用户可全删重写），仅作写 prompt 的起点，不生成任何已建角色：
```
你是<角色名>。请基于<维度>审阅下面这篇成稿。
要求：使用 <评审意见> 标签包裹你的输出；
问题列表每条必须含 位置 ｜ 严重度(高/中/低) ｜ 问题 ｜ 建议 四要素；
最后给一句总体建议。
```
> 角色 prompt 可调用的上下文：成稿（必填）+ 可选 `<账号定位>`/`<框架>`/`<素材>`（系统按本次任务勾选注入）。

**角色管理**：增 / 删 / 改 / 拖拽排序；支持**导出/导入角色集**（JSON，便于备份与跨机迁移）。**无"恢复默认"**（本就无默认）。

### 5.2 AI 输入（评审一次调用 = 一个角色）

```xml
<成稿> …（Markdown 全文，必填） </成稿>
<账号定位> … </账号定位>   （可选，按任务勾选注入，调性对齐/合规参考）
<框架> … </框架>           （可选，结构对齐参考）
<素材> … </素材>           （可选，事实回溯来源参考）
<评审维度> 自由文本，该角色聚焦的维度 </评审维度>  （可选，提示模型；由 prompt 决定）
```

> 一次调用只审一个角色，调用使用的模型 = 该角色 `modelId`（空则全局默认）。N 个角色 = N 次并行独立调用（非一个 prompt 里塞多个角色——保证各视角独立、互不稀释，也便于单角色失败隔离）。

### 5.3 AI 输出（严格对齐写文章 PRD §4.3 契约）

```xml
<评审意见>
评审角色：事实核查官
维度：事实 / 可读性 / 调性 / 合规 / 传播
问题列表：
  - 位置：第二段 ｜ 严重度：中 ｜ 问题：数据无来源 ｜ 建议：补充来源或弱化表述
  - 位置：标题 ｜ 严重度：低 ｜ 问题：钩子偏弱 ｜ 建议：增加悬念
总体建议：核心论点清晰，建议强化开头共鸣。
</评审意见>
```

- **维度行**：列出该角色覆盖的维度（可多个，自由文本）。
- **问题列表**：每条固定四要素 `位置 ｜ 严重度 ｜ 问题 ｜ 建议`，严重度取 `高/中/低`（**无阻塞级**，是否要改由用户自行判断）。
- **总体建议**：一句话总结。
- 输出解析：系统按 XML 标签解析为结构化记录，便于逐条渲染"采纳/忽略"勾选。

#### 5.3.1 程序化提取 `<评审意见>`（抗 CoT / 噪声）

模型常在正式结果前输出大量思考/解释内容，直接 JSON/XML 解析会失败或误吞噪声。**本模块的提取交由模型网关统一处理**（见 `技术规格-模型网关.md` §10.1 `extractBlock`），不再各角色自行实现：

- 调用 `gateway.chat()` 时传入 `extractBlock: { tag: "评审意见", occurrence: "last" }`；网关在返回里给出 `extracted`（块内内容）+ `extractionMatched`。
- **规则可自定义**：角色配置的 `extractionRule`（标签名 / 取第几块 `occurrence` / 是否保留标签 `includeTags`）即映射为该角色专用的 `extractBlock` 参数，用户可改成不同标签名或取首个块。
- **兜底**：若 `extractionMatched=false`（未匹配到 `<评审意见>`），整段原始 `content` 作为 `rawXml` 直拼改稿指令（见 §7 风险缓解），并在界面提示"未提取到标准评审块，请人工核对"。

> **已泛化（用户确认）**：此"截 XML 块再解析"模式对所有 XML 包裹输出的 AI 角色（账号定位 / 选题 / 框架 / 写文章 / 配图策略 / 配图提示词 / 评审）统一生效——已在模型网关层作为公共 `extractBlock` 能力实现，各角色只传 `tag` 即可，避免重复造轮子。

### 5.4 多角色并行与聚合

- 用户在成稿上勾选 K 个角色 → 系统发起 K 次并行独立调用（同一成稿，不同角色 prompt、可能不同模型）。
- 结果以 **K 份独立 `<评审意见>`** 呈现（每份标注角色名、维度、所用量模型、耗时）。
- **不做**自动"综合汇总一份"——保持角色视角独立，采纳决策交还用户。
- 单角色调用失败：该份标记"评审失败，可重试"，不影响其余。

### 5.5 评审结果工作台（采纳 / 忽略 / 编辑 / 追加）

- **逐条操作**：每条问题可独立 `采纳` / `忽略`，默认全采纳（勾选态）。
- **严重度可视**：高=红 / 中=黄 / 低=灰，便于一眼看优先级（无阻塞级，颜色仅作提示）。
- **手动编辑**：用户可改某条的位置/严重度/问题/建议（人审补刀）。
- **手动追加**：用户可新增一条意见（纯文本，角色名标"人工"），同样进改稿指令。
- **角色级折叠**：同一角色的若干条问题折叠在该角色卡片下，纵向排列。
- **汇总视图**：顶部显示"共 K 份评审 / M 条问题 / 高严重度 X 条"，供全局判断。

### 5.6 触发改稿（一键应用评审，无预览）

- 用户点 **"应用评审改稿"** → 系统把所有 `采纳` 状态（含用户追加）的 `<评审意见>` **经各自提取规则确认后的内容**序列化拼入一个 `<修改指令>`：
  ```
  <修改指令>
  <评审意见> …（事实核查官，采纳的问题）… </评审意见>
  <评审意见> …（可读性编辑，采纳的问题）… </评审意见>
  …（仅采纳的那些）
  </修改指令>
  ```
- **不弹改稿预览**，直接调用写文章 AI 的 `revise` 模式（见写文章 PRD §4.2）→ 产出新成稿版本（支持 count 候选对比）。
- 改稿完成，评审任务标记"已应用"；原成稿保留版本历史可回滚。
- **一轮即终**：应用一次即结束，不自动回评（无多轮循环）。若用户不满意改稿结果，可手动再发起一轮评审（非强制流程）。

### 5.7 与上下游关联（非耦合）

```
[成稿] ──────► 评审 AI（K 个用户自建角色并行）
                │  产出 K 份 <评审意见>（各经 extractionRule 提取）
                ▼
         评审结果工作台（采纳/忽略/编辑/追加）
                │  "应用评审改稿"（无预览）
                ▼
         <修改指令>（拼入采纳意见）──► 写文章 AI revise ──► 新成稿版本
```

- 评审任务作为成稿的**关联产物**（外键 `articleId`），可空、非强制。
- 评审不写回成稿正文，只产出意见；改稿动作由写文章模块执行（职责清晰）。
- 可参考上下文：`<账号定位>`（调性/合规）、`<框架>`（结构）、`<素材>`（事实来源）——均为可选软引用，按任务勾选注入。

---

## 6. 数据模型（概要）

```text
评审任务 ReviewTask:
  id, articleId (关联成稿，必填)
  selectedRoleIds: []          # 本次选中的角色
  status: 草稿 / 进行中 / 已完成 / 已应用
  createdAt, updatedAt

评审角色 ReviewRole:           # 用户自建，无默认
  id, name
  systemPrompt
  modelId?                     # 该角色模型；空=全局默认评审模型
  extractionRule?              # 截 <评审意见> 的规则；空=默认正则
  order                        # 展示顺序

评审意见 ReviewOpinion:
  id, taskId, roleId, roleName, modelUsed
  dimensions: []               # 该份覆盖维度（自由文本）
  problems: [                 # 问题列表
    { position, severity(高/中/低), issue, suggestion, adopted(boolean) }
  ]
  overallSuggestion
  rawXml                      # 原始 <评审意见> 文本（已提取后），供改稿直接拼入

# 关联上下文（软引用，可选）：
  accountId?, frameworkId?, materialIds?: []
```

- 评审意见存结构化字段 + `rawXml`（保留原始 XML 便于拼改稿指令，与全局 XML 约定一致）。
- 角色集本地存储于配置库（与账号字段 schema、选题 schema、框架模板同属"可配置定义"层）；支持导出/导入 JSON。

---

## 7. 风险与缓解

| 风险 | 影响 | 缓解 |
|------|------|------|
| 多角色并行成本高（K 次调用，且可能不同模型） | token/费用上升 | 角色模型可为空回退全局默认；用户自控角色数；成本只在本地用量记录（无单价表） |
| 用户自建 prompt 弱导致意见空泛 | 评审无用 | 角色创建引导模板内嵌"必须用 `<评审意见>` 包裹、给具体位置+可操作建议"约束；用户可迭代改 prompt |
| 模型先吐思考内容再给 XML | 解析失败/吞噪声 | §5.3.1 **可配置提取规则**先截块再解析；未匹配则 rawXml 直拼并提示人工核对 |
| 严重度主观、角色间冲突 | 用户困惑 | 各角色独立呈现，不强行调和；无阻塞级，用户自行取舍 |
| 改稿过度改写（评审意见太多） | 成稿面目全非 | 写文章 revise 约束"最小必要改动"；用户可只采纳高/中严重度 |
| `<评审意见>` 格式漂移 | 改稿接口接不上 | 输出严格对齐 §4.3；提取规则兜底；解析失败 rawXml 直拼 |
| 合规审查误判/漏判 | 发布风险 | 合规角色仅提示风险，最终判断与责任在用户；界面标注"AI 不替代法律/合规终审" |

---

## 8. 验收标准（Definition of Done）

1. 角色库初始为空；用户可创建角色（名称 + systemPrompt + 可选模型 + 可选提取规则），可改/删/排序/导出导入。
2. 用户可选中 1–K 个自建角色，对一篇成稿发起评审，系统并行产出 K 份独立 `<评审意见>`（各用各自模型）。
3. 每份输出**严格符合**写文章 PRD §4.3 的 `<评审意见>` 结构，系统经**该角色 extractionRule 提取后**可解析为结构化记录。
4. 模型返回含前置思考内容时，提取规则能正确截出 `<评审意见>` 块，未匹配时 rawXml 直拼并提示人工核对。
5. 每条问题可独立 `采纳/忽略`，用户可编辑、可手动追加意见（角色名"人工"）。
6. 点"应用评审改稿"后（**无预览**），采纳的意见正确拼入 `<修改指令>`，成功调用写文章 `revise`，生成新成稿版本（保留旧版本可回滚）。
7. **一轮即终**：应用后评审任务标记"已应用"，无自动回评循环。
8. 评审任务作为成稿关联产物存储，可重看历史评审。
9. 单角色调用失败可单条重试，不影响其余。

---

## 9. 开放问题（已决，归档）

1. **默认角色集**：→ 用户决策**无默认**，角色完全自建（§5.1）。
2. **严重度分级**：→ 维持 高/中/低，**不加阻塞级**，用户自行确认（§3/§5.3）。
3. **汇总角色**：→ MVP 不做，保持 K 份独立（§5.4）。
4. **应用时机**：→ **不弹预览**，点"应用评审改稿"直接改稿（§5.6）。
5. **角色级模型**：→ **允许每角色指定不同模型**，空则回退全局默认（§5.1/§5.2）。

---

*下一步*：模块八封板后，P1 剩余 = 配图三件套（策略 AI / 提示词 AI / 生成 AI，模块九~十） → 排版 AI（模块十一）。至此全链路（账号→热点→选题→框架→写作→评审→配图→排版）需求侧闭环。
