# PRD · 配图三件套（配图策略 AI / 配图提示词 AI / 配图生成 AI）

> 文档状态：Draft v0.2 ｜ 范围：单阶段深度定义（含 3 个 AI 角色：模块九/十/十一）｜ 平台：Windows 本地桌面应用（无云端）
> 关联：上游消费「成稿」（模块五，已锁版或草稿均可，关联软引用）；下游供「排版 AI」（模块十二）组装最终稿。配图生成调用 `技术规格-模型网关.md` 的 `generateImage()`。本阶段所有 XML 输出统一走网关 `extractBlock`（§10.1）抗 CoT 截取。
> 平台约束：Windows 本地应用，图片资产存本地文件，API Key 不出本机。
>
> **流程顺序（用户已定）**：文章成型 → 配图策略（从文章提取配图点）→ 配图提示词 → 配图生成 → 排版（模块十二，在最后组装）。

---

## 1. 背景与 Why

成稿（模块五）是纯文字，公众号/小红书等平台对"图文并茂"有刚需。文章的配图不能靠人肉盲找——AI 应当：① **读懂文章哪里该配图**、② **把配图意图翻译成图像模型能吃的提示词**、③ **调图像模型出图**。

这三者是流水线的最后一段"内容增强"，且**强依赖图像模型**（不同于前面 11 个 LLM 角色）。它们与排版解耦：配图产出的是"图片资产 + 配图点定位"，排版负责最终组装。

---

## 2. 目标（Goals）/ 非目标（Non-goals）

**Goals**
- 从成稿**自动提取配图点**（位置/类型/场景/尺寸建议/风格约束）——配图策略 AI。
- 把每个配图点**翻译成图像模型提示词**（可编辑、可设风格）——配图提示词 AI。
- 调**网关 `generateImage()`** 出图，资产落本地——配图生成 AI。
- 每环节**人工可干预**（改配图点 / 改提示词 / 换模型重生成 / 选候选）。
- 风格统一：支持"配图风格"配置，保证一个账号的图调性一致。
- 配图资产**可溯源**到具体配图点、可关联到排版。

**Non-goals（防范围蔓延）**
- 不做"AI 直接出排版好的图文长图"（那是排版 AI 职责）。
- 不做图像**编辑/美工**（裁剪/加字/去背景）——只出原图；如需，留给外部工具或后续模块。
- 不做图像**版权确权/水印**——仅标注风险（见 §7），不做法律动作。
- 不内置图像模型——统一走网关 `generateImage()`，模型由用户配。
- 不做"根据文章自动插入图到 Markdown 正文"的强制行为——默认只产出资产 + 配图点，插入由排版/用户决定（可选提供"插入成稿"轻操作）。

---

## 3. 用户故事

- 作为 **内容创作者**，我希望 AI 告诉我"这篇文章哪些位置该配图、配什么图"，以便不用自己瞎想配图方案。
- 作为 **内容创作者**，我希望每个配图点能一键生成"图像模型提示词"且我能改，以便控制出图效果。
- 作为 **内容创作者**，我希望直接调我配的图像模型出图、并能换模型/重生成/选候选，以便拿到满意的图。
- 作为 **账号运营者**，我希望配图风格和账号调性一致（如统一扁平插画），以便视觉统一。

---

## 4. 整体设计（三件套串联）

```
成稿(Markdown)
   │ 选成稿 + 可选选账号(风格参考)
   ▼
[模块九 配图策略 AI]  →  <配图策略>(配图点列表)   ── 草稿→锁定,可改
   │ 选配图点(可多选)
   ▼
[模块十 配图提示词 AI] →  <配图提示词列表>          ── 草稿→锁定,可改 prompt
   │ 选提示词(可多选)
   ▼
[模块十一 配图生成 AI] →  调 gateway.generateImage() → 图片资产(本地) ── 可重生成/选候选
   │ 资产关联到对应配图点
   ▼
[模块十二 排版 AI] 组装「文字 + 图片资产 + 样式模板」→ 最终排版稿
```

**松耦合**：三件套都是成稿的关联产物（非锁定硬依赖）。可从任意环节启动（如手建配图点、或跳过策略直接给提示词出图）。资产与配图点关联，供排版消费。

### 4.1 模块九：配图策略 AI（提取配图点）

**输入**（XML 包裹纯文本）
```
<成稿> … </成稿>              （必填，Markdown 原文）
<账号定位> … </账号定位>      （可选，提供风格/IP 参考；accountIds 为空时不带）
```
- 调用 `gateway.chat({ ..., extractBlock: { tag: "配图策略", occurrence: "last" } })`。

**输出 `<配图策略>`**（每个配图点一个 `<配图点>` 块）
```
<配图策略>
<配图点>
位置：第2段之后
类型：信息图
场景描述：用一张图解释"注意力机制"的三个步骤
尺寸建议：16:9
风格约束：扁平插画，蓝橙配色，与账号调性一致
配图理由：此处概念抽象，配图可显著提升理解
</配图点>
<配图点>
位置：结尾之前
类型：金句卡片
场景描述：把核心观点做成可截图转发的卡片
尺寸建议：1:1
风格约束：大字居中，留白
配图理由：增强转发率
</配图点>
</配图策略>
```
- 字段说明：**位置**（插入锚点，如"第 N 段之后/标题下/结尾"）、**类型**（信息图/示意图/封面/金句卡片/表情包…自由文本）。**封面图单独区分**：封面是独立类型，默认尺寸建议 **2.35:1**（适配公众号首图/分享卡等），生成与排版时与正文配图分别处理（封面用于顶部/分享，正文配图插入正文）。**场景描述**（给提示词 AI 的原材料）、**尺寸建议**（给图像模型的 size 提示）、**风格约束**（从账号定位粗略继承，提示词 AI 再细化）、**配图理由**（人审参考）。

### 4.2 模块十：配图提示词 AI（配图点 → 图像提示词）

- **输入**：选中的 1 个或多个 `<配图点>` + **配图风格**（配置）+ 可选 账号定位。
```
<配图点> … </配图点>         （必填，来自模块九；可批量传入多个）
<配图风格> … </配图风格>      （必填，当前生效的配图风格配置文本）
<账号定位> … </账号定位>      （可选）
```
- 调用 `gateway.chat({ ..., extractBlock: { tag: "配图提示词列表", occurrence: "last" } })`。

**输出 `<配图提示词列表>`**（每个配图点对应一个 `<配图提示词>`）
```
<配图提示词列表>
<配图提示词>
关联配图点：第2段之后
提示词：flat illustration, three-step diagram of attention mechanism, blue-orange palette, minimal, no text
尺寸：16:9
风格：扁平插画
数量：2
</配图提示词>
<配图提示词>
关联配图点：结尾之前
提示词：quote card, bold centered Chinese typography, "核心观点...", large whitespace, beige background
尺寸：1:1
风格：金句卡片
数量：1
</配图提示词>
</配图提示词列表>
```
- **提示词语种**：用户决策——**提示词保持中文**（"中文→英文"翻译开关**默认关**，不做翻译）。即梦/豆包等原生中文图像模型直接吃中文 prompt；若用户改用 DALL·E/Imagen 等英文更稳的模型，可自行打开翻译开关。
- 字段：**关联配图点**（溯源锚定）、**提示词**（直接喂 `generateImage` 的 `prompt`）、**尺寸**（→ `size`）、**风格**（→ 提示词补充/即梦 style 参数）、**数量**（→ `count`）。

### 4.3 模块十一：配图生成 AI（调网关出图）

- **输入**：选中的 1 个或多个 `<配图提示词>`（来自模块十，已锁定或草稿）。
- **调用**：对每个提示词调 `gateway.generateImage({ model, prompt, size, count, style })`。
  - `model` 默认从"当前默认图像 Provider"（网关全局），可在生成面板逐条/批量改。
  - `size` 取提示词里的尺寸，`count` 取数量，`style` 取风格（即梦/Imagen 支持时传）。
- **输出**：图片资产，存本地（如 `AppData\Local\...\assets\images\{uuid}.png`）。
  - DB 记录：`image_asset(id, prompt_id, path, model, size, seed?, revised_prompt?, created_at, version)`。
  - 资产与对应 **配图点** 关联（通过 prompt_id → 配图点）。

**HITL / 重生成**
- 每提示词可生成 **count 个候选**（如 2 张），用户选 1 张定稿，其余留作候选。
- **换模型重生成**：改 `model` 重跑，产生新版本（不丢已选）。
- **改提示词重生成**：回到模块十改 prompt 再生成。

### 4.4 配图风格配置（可配置定义层）

- 与"账号字段 schema / 选题 schema / 框架模板"同属一层"可配置结构定义"，本地存储。
- 结构：`{ name, description }`，`description` 为自由文本（如"扁平插画，明亮色调，无文字，圆角"），注入配图提示词 AI 的 system prompt。
- MVP：**默认 1 个全局配图风格**（可改名/编辑），支持保存多个并切换。
- **风格不绑定账号**：风格是独立实体，通过"关联"形式被使用（如配图任务/生成时选择具体风格），而非按账号强制绑定。账号调性的一致性由用户在选择风格时自主保证。

---

## 5. 交互与呈现

- **配图策略工作台**：左侧成稿预览，右侧 `<配图策略>` 卡片列表（每卡 = 一个配图点，折叠显示位置+类型+理由，展开看全字段）。可增删配图点、改字段、拖拽调顺序。锁定后供提示词 AI 消费。
- **提示词工作台**：每个配图点 → 一个提示词编辑卡（提示词文本框可改、尺寸/风格/数量可改、中文→英文开关）。批量选中 → 调提示词 AI 生成 / 已生成则展示。
- **生成工作台**：每个提示词 → count 张候选图网格，点选定稿；重生成/换模型按钮；资产列表（按配图点分组）。
- **状态机放宽口径沿用**：锁定非下游硬门槛，三件套均可从中间环节启动。

---

## 6. 与网关 / 全局约定的关系

- 三件套的文本类输出（`<配图策略>` / `<配图提示词列表>`）均为 **XML 包裹纯文本**，且统一经网关 **`extractBlock`** 截取（标签名 `配图策略` / `配图提示词列表`），抗模型 CoT 噪声——这是 v0.2 已泛化的公共能力，本模块**不再自行实现解析**（见 `技术规格-模型网关.md` §10.1）。
- 图像生成仅调 `gateway.generateImage()`，适配差异沉在网关图像适配器（DALL·E/即梦/Imagen/SD/MJ 代理），本模块不感知厂商。
- 本地存储图片资产与 DB 记录，不触云。

---

## 7. 风险与缓解

| 风险 | 影响 | 缓解 |
|------|------|------|
| 图像模型内容被拦（敏感图） | 出图失败 | 网关 `ContentPolicyError` 返回原因，引导改 prompt；不静默 |
| 中文 prompt 在 DALL·E/Imagen 效果差 | 出图不符 | 默认中文 prompt（用户决策）；若用 DALL·E/Imagen 可手动开"中文→英文"翻译开关 |
| 配图点位置锚定不准（文章改了） | 图插错位置 | 位置用"第 N 段之后"软锚，插入排版时二次确认；文章大改后建议重提策略 |
| 图片资产版权 / 平台 AI 标识要求 | 合规风险 | 标注风险（公众号/小红书对 AI 图可能需标识）；不做确权，提示用户自查 |
| 图像模型协议碎、MJ 代理不稳 | 生成失败 | 主推 DALL·E/即梦/Imagen/SD；MJ 走代理 P1，稳定性自担（网关层已隔离） |
| 多候选占本地空间 | 存储 | **定稿后 30 天自动清理未勾选（未选）候选图**；已选/定稿资产保留（清理策略可开关） |
| 配图风格与账号调性脱节 | 视觉不一致 | 配图风格配置注入提示词；可选关联账号定位风格 |

---

## 8. 成功指标 / 验收标准

**验收标准**
1. 选成稿 → 配图策略 AI 输出 ≥1 个 `<配图点>`，结构符合 §4.1，提取经 `extractBlock` 成功（`extractionMatched=true`）。
2. 选配图点 → 配图提示词 AI 输出对应 `<配图提示词>`，含可喂 `generateImage` 的 `prompt`/`size`/`count`，提取成功。
3. 选提示词 → `generateImage()` 对 ≥1 类图像模型（如 DALL·E 或 即梦）成功出图，资产存本地、DB 有记录、关联到配图点。
4. 每环节支持**草稿→锁定**、字段编辑、换模型/改提示词重生成、count 候选选稿。
5. 配图风格配置可改并注入提示词 AI。
6. 三件套任意环节可独立启动（松耦合），不强制从策略开始。
7. 下游排版可消费"配图点 + 图片资产"完成组装（接口预留，详见模块十二）。

---

## 9. 开放问题（已全部拍板，见 v0.2 决策）

1. **配图风格默认数量**：✅ MVP 默认 1 个全局风格，可创建多个；**风格不绑定账号**，以独立实体通过"关联"形式被使用（生成时选择），账号调性一致性由用户选风格时自主保证。
2. **提示词语种**：✅ **提示词保持中文**（"中文→英文"翻译开关默认关）；即梦/豆包等原生中文模型直接吃中文，用 DALL·E/Imagen 时可手动开翻译。
3. **未选候选清理**：✅ **定稿后 30 天自动清理未勾选（未选）候选图**；已选/定稿资产保留。清理策略可开关。
4. **"插入成稿"轻操作**：✅ 提供（按 PM 建议）——每个配图点（或批量）提供"插入成稿"按钮，**默认关**，不强制；点击按配图点位置软锚（"第 N 段之后"）把 `![](path)` 插入当前成稿 Markdown 副本。轻操作、可撤销、不动已锁定成稿。封面类型图单独处理（不自动插入正文，留给排版/手动放置）。
5. **封面图**：✅ **单独区分**——封面是独立类型，默认尺寸建议 2.35:1（公众号首图/分享卡），生成与排版时与正文配图分通道处理。

---

*下一步*：模块十二 排版 AI（定义统一风格模板 → 组装「文字 + 图片资产 + 样式」→ 本地导出 md/html/图）。全链路需求侧闭环待排版封板。
