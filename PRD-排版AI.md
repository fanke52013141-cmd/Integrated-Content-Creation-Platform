# PRD · 排版 AI（模块十二 / 业务环节 9，终环）

> 文档状态：Draft v0.3 ｜ 范围：单模块深度定义（含 1 个 AI 角色 + 1 个开源渲染内核）｜ 平台：Windows 本地桌面应用（无云端）
> 关联：上游消费「成稿」（模块五，Markdown）+「配图资产」（模块九~十一，正文配图 + 封面）+「排版模板」（可配置定义层）；下游产出本地导出文件（公众号 HTML / 本地 MD/HTML）。所有 LLM 输出统一走网关 `extractBlock`（§10.1）抗 CoT 截取。
> 平台约束：Windows 本地应用，数据本地存储，API Key 不出本机；**渲染内核用开源项目 md2wechat（npm / MIT），本地进程内调用，无云端依赖**。
>
> **流程顺序（用户已定）**：文章成型 → 配图（策略/提示词/生成）→ **排版（模块十二，最终组装，在最后）**。
> **本模块两项关键决策（2026-07-27 拍板）**：
> 1. **渲染内核 = 开源 md2wechat**（npm 包、内置 Agent API、MIT；多主题 + 40+ 代码高亮 + KaTeX 公式），本地进程内调用，不自研渲染。
> 2. **AI 职责 = 轻编排**：按配图点把图插入正文 + 选/推荐排版模板 + 处理封面位 + 可选生成导读/结尾互动语；**不改写正文**（写文章已负责写，排版不越界）。
> 3. **导出形态 = 公众号 HTML（一键复制粘贴）+ 本地 MD/HTML 备份**；小红书长图（HTML→图）留 P2。

---

## 1. 背景与 Why

流水线的前 11 个环节把"成稿 + 配图资产"都准备好了，但成品还不能直接发——公众号/知乎等后台对"图文样式"有强诉求（字体/配色/卡片/代码高亮/公式），而纯 Markdown 直接粘贴会格式错乱、代码块惨不忍睹。

**排版 AI 的本质是"最终组装"**：把「文字（成稿）+ 图片资产（正文配图 + 封面）+ 样式模板」拼成一个"能直接发"的成品。它**不是改写器**——正文由写文章负责，配图由配图负责，排版只做"编排 + 套样式"。

用户决策明确：**渲染层直接吃成熟开源引擎（md2wechat）**，我们只让 AI 做轻量编排判断（图放哪、套哪个样式、封面位、可选导读），重活交给开源项目，开发省力、样式稳。

---

## 2. 目标（Goals）/ 非目标（Non-goals）

**Goals**
- 消费「成稿 Markdown」+「配图资产（正文图 + 封面）」+「排版模板」，产出**可发布排版稿**。
- AI **轻编排**：① 按配图点把正文图落位；② 选/推荐排版模板；③ 处理封面位（2.35:1）；④ 可选生成导读/结尾互动语。
- **渲染走开源 md2wechat**：成稿（已插图）+ 模板（CSS 主题）→ 公众号兼容 HTML / 本地 MD/HTML。
- 每环节**人工可干预**：改插入点、换模板、改导读、重渲。
- **排版模板可配置**（定义层，同配图风格）：默认 1 全局、可多套、不绑账号、关联使用。
- 导出：公众号 HTML 一键复制粘贴 + 本地 MD/HTML 备份。

**Non-goals（防范围蔓延）**
- 排版 AI **不重写/扩写正文**（那是写文章职责）——仅可加"导读/结尾互动语"这类轻量风格文案。
- 排版 **不生成图片**（那是配图三件套职责）——仅消费已生成的图片资产。
- **不做小红书长图渲染**（HTML→图片，需无头浏览器，留 P2）。
- **不做一键发布到各平台后台**（本地应用，手动复制；微信草稿接口需服务端授权，超出本地范围）。
- **不内置自研渲染引擎**——统一用开源 md2wechat；样式差异沉在 md2wechat 主题机制。
- **不做多账号模板绑定**——模板是独立实体，通过"关联"被使用（同配图风格口径），账号一致性由用户选模板时自主保证。
- **不做配图落位的强制自动插入**——AI 出"排版方案"草稿，用户可在工作台拖拽调插入点（HITL），非硬插入。

---

## 3. 用户故事

- 作为 **内容创作者**，我希望 AI 把配图按合适位置插进文章、套上和账号调性一致的样式，以便不用自己调格式。
- 作为 **内容创作者**，我希望排版后一键复制出"公众号能直接粘"的 HTML，以便省去手动调样式的功夫。
- 作为 **账号运营者**，我希望排版模板和账号调性一致（如统一科技蓝），以便视觉统一、复用。
- 作为 **内容创作者**，我希望能改插入点、换模板、改导读，再重渲，以便拿到满意成品。

---

## 4. 整体设计（AI 编排 + 开源渲染）

```
成稿(Markdown, 模块五)
   + 配图资产(正文配图 + 封面 2.35:1, 模块九~十一)   ← 经 配图点(软锚)关联
   + 排版模板(可配置定义层)
   │
   ▼
[模块十二 · 排版 AI]  →  <排版方案>(配图插入点 + 推荐模板 + 导读/互动语)  ── 草稿→锁定,可改
   │  (确定性渲染，无 LLM)
   ▼
[渲染内核 · md2wechat]  →  公众号兼容 HTML + 本地 MD/HTML  ── 一键复制 / 本地导出
```

**松耦合**：排版是成稿 + 配图资产 + 模板的关联产物（非锁定硬依赖）。无配图也能排版（仅文字 + 模板）；无模板也能排版（用默认模板）。

### 4.1 模块十二·排版 AI（轻编排，LLM）

**输入**（XML 包裹纯文本）
```
<成稿> … </成稿>                       （必填，Markdown 原文，来自模块五；草稿/锁定均可）
<配图点列表> … </配图点列表>           （可选；来自模块九，含"第 N 段之后"软锚；无配图则空）
<配图资产> … </配图资产>               （可选；与配图点关联的正文图 path + 封面 path，来自模块十一）
<账号定位> … </账号定位>               （可选，风格参考，用于模板推荐 / 导读语气的基调）
<排版模板> … </排版模板>               （可选；用户已选的模板；空则 AI 推荐）
```
- 调用 `gateway.chat({ ..., extractBlock: { tag: "排版方案", occurrence: "last" } })`。
- **模型**：用全局默认 LLM（网关全局），本模块不做逐任务模型选择（轻任务，保持简单；后续如需可加）。

**输出 `<排版方案>`**（纯文本，字段用"键：值 / 列表"约定，不写死 JSON）
```
<排版方案>
配图插入点：
  - 图片：assets/images/{uuid-A}.png ｜ 位置：第2段之后 ｜ 说明：信息图解释注意力机制
  - 图片：assets/images/{uuid-B}.png ｜ 位置：结尾之前 ｜ 说明：金句卡片
推荐模板：科技蓝 ｜ 理由：账号偏科技理性，蓝色系契合正文调性
封面位：assets/images/{uuid-cover}.png ｜ 说明：2.35:1 首图，用于公众号顶部/分享卡
导读：大模型又进化了，普通人该慌吗？先别急——这篇文章给你三个视角的答案。
结尾互动语：你最担心被 AI 替代的是哪部分工作？评论区聊聊。
</排版方案>
```
- 字段说明：
  - **配图插入点**：AI 把每个正文图映射到成稿的**段落锚点**（因成稿可能在配图后改过，故 AI 基于当前成稿**重新语义推导落位**，不机械依赖配图点的旧软锚）。引用图片用本地 `path`（与配图资产关联）。
  - **推荐模板**：若用户未选模板，AI 基于 `<账号定位>`/文章调性推荐一个（给理由）；用户可覆盖。
  - **封面位**：填入封面资产 path（2.35:1，来自配图模块封面类型）。无封面则空。
  - **导读 / 结尾互动语**：仅当对应开关开启时生成（默认开，轻量风格文案）；**不触碰正文**。导读置于文首、互动语置于文末，由渲染步骤插入。
  - 关于"金句卡片"图：配图模块已可生成金句卡片**图片**，其落位走"配图插入点"；此处"结尾互动语"是**文字**，二者不冲突（一个是图、一个是文末引导语）。

### 4.2 渲染内核 · md2wechat（确定性，无 LLM）

- **调用**：本地进程内 `require('md2wechat')`（npm / MIT），传入：
  - 成稿 Markdown（已按 `<排版方案>` 的"配图插入点"把 `![](path)` 插入对应段落；导读/互动语插文首/文末）；
  - 排版模板（→ md2wechat 主题 key + 可选自定义 CSS 覆盖）；
  - 封面位（→ 模板封面槽，公众号首图）。
- **输出**：
  1. **公众号兼容 HTML**：内联样式、代码高亮、公式渲染，可直接 Ctrl+V 粘进公众号后台（md2wechat 原生能力）。
  2. **本地 MD/HTML 备份**：存 `exports/{slug}.md` / `exports/{slug}.html`（带图片本地引用）。
- **样式来源**：md2wechat 内置多主题 + 代码高亮 + KaTeX；"排版模板"定义层 = `{ name, baseTheme(主题key), customCss?(可选覆盖), coverSlot(默认true) }`。

> **集成假设（工程实现时校验）**：md2wechat 暴露「主题选择 + 可选自定义 CSS 注入」接口。若其自定义 CSS 入口形态不同，PRD 口径不变——渲染仍走 md2wechat，自定义样式以该库支持的覆盖机制落地。此点列为实现核对项（§9 开放问题）。

### 4.3 排版模板配置（可配置定义层）

- 与"账号字段 schema / 选题 schema / 框架模板 / 配图风格"同属一层"可配置结构定义"，本地存储。
- 结构：`{ name, description, baseTheme, customCss?, coverSlot }`；`description` 自由文本（如"科技蓝，正文 15px，行距 1.75，卡片圆角"），`baseTheme` 为 md2wechat 主题 key，`customCss` 可选覆盖。
- MVP：**默认 1 个全局排版模板**（可改名/编辑，关联一个 md2wechat 主题），支持保存多个并切换。
- **模板不绑定账号**：独立实体，通过"关联"被使用（排版任务选具体模板），账号一致性由用户选模板时自主保证（同配图风格口径）。

---

## 5. 交互与呈现

- **排版工作台**：
  - 左：成稿预览（含配图资产缩略图列表）；中：`<排版方案>` 可编辑（插入点列表可拖拽调顺序/改锚点、模板下拉、导读/互动语文本框）；右：实时渲染预览（调用 md2wechat 渲染）。
  - **插入点 HITL**：用户可在预览里把图片拖到具体段落，覆盖 AI 落位（非硬插入）。
  - 模板下拉：选已存模板或"让 AI 推荐"（触发 排版 AI 重跑）。
  - 封面槽：拖入/选择封面资产（2.35:1）。
- **导出区**：「复制到公众号」（剪贴板写入 md2wechat 产出的公众号 HTML）+「导出本地 MD/HTML」按钮。
- **状态机放宽口径沿用**：锁定非下游硬门槛；排版可从成稿/配图任意中间态启动。

---

## 6. 与网关 / 全局约定的关系

- 排版 AI 的文本输出 `<排版方案>` 为 **XML 包裹纯文本**，经网关 **`extractBlock`** 截取（标签名 `排版方案`），抗模型 CoT 噪声——v0.2 已泛化的公共能力，本模块**不再自行实现解析**（见 `技术规格-模型网关.md` §10.1）。
- 渲染（md2wechat）是**确定性本地调用，不进网关、不耗 LLM**。
- 图片资产本地读取，不触云；封面 2.35:1 沿用配图模块口径。

---

## 7. 风险与缓解

| 风险 | 影响 | 缓解 |
|------|------|------|
| md2wechat 主题/CSS 覆盖接口形态不符预期 | 自定义样式落地偏差 | §9 列实现核对项，工程联调时确认；PRD 口径不变（仍走 md2wechat） |
| 配图落位锚点不准（成稿改过） | 图插错段落 | AI 基于当前成稿重推导；用户可在工作台拖拽覆盖 |
| 公众号 HTML 粘贴后样式微调（微信后台二次处理） | 个别样式失真 | md2wechat 已针对公众号兼容优化；导出本地 HTML 留底可比对 |
| 封面缺失 | 公众号首图空 | 封面槽可空；提示用户补封面（配图模块可单独出封面） |
| 导读/互动语与正文调性不符 | 风格违和 | 导读/互动语生成带 `<账号定位>` 基调约束；用户可手改 |
| 小红书长图需求 | 暂不满足 | 留 P2（HTML→图需无头浏览器，本模块不做） |
| 多模板/多账号一致性 | 视觉不统一 | 模板独立实体、关联使用；用户选模板时自主保证（同配图风格） |

---

## 8. 成功指标 / 验收标准

**验收标准**
1. 选成稿（+ 可选配图资产 + 可选模板）→ 排版 AI 输出 `<排版方案>`，含配图插入点 +（推荐）模板 +（可选）导读/互动语，提取经 `extractBlock` 成功（`extractionMatched=true`）。
2. 渲染（md2wechat）消费「成稿(已插图) + 模板(CSS)」→ 产出**公众号兼容 HTML**（内联样式、代码高亮、公式）可被公众号后台粘入；同时落本地 MD/HTML。
3. 每环节 HITL：插入点可拖拽改、模板可换、导读/互动语可改、可重渲。
4. 无配图时仅文字 + 模板也能排版；无模板时用默认模板也能排版。
5. 排版模板配置可增删改并注入渲染（baseTheme + 可选 customCss）；不绑账号、关联使用。
6. 封面资产（2.35:1）可入封面槽；缺封面可空。
7. 排版方案支持草稿→锁定、版本历史、可回滚（沿用全局 HITL + 版本机制）。
8. 「复制到公众号」「导出本地 MD/HTML」功能可用。

**成功指标**

| 指标 | 类型 | 目标 |
|------|------|------|
| 排版渲染成功率（md2wechat 出 HTML 无异常） | 健康 | ≥ 99% |
| 排版方案提取成功率（extractBlock matched） | 健康 | ≥ 98% |
| 配图落位采纳率（用户未改插入点即导出） | 驱动 | 观察 |
| 模板复用率（非默认模板被选用次数） | 驱动 | 观察一致性价值 |
| 一键复制→公众号粘贴保真度（人工抽检） | 健康 | 无重大失真 |
| 导出本地文件成功率 | 健康 | 100% |

---

## 9. 开放问题（已拍板项 + 1 实现核对项）

1. **渲染内核选型**：✅ **md2wechat（npm / MIT）**，本地进程内调用，不自研；多主题 + 代码高亮 + KaTeX。
2. **AI 职责深度**：✅ **轻编排**——配图落位 + 选/推荐模板 + 封面位 + 可选导读/结尾互动语；不改写正文。
3. **导出形态**：✅ **公众号 HTML（一键复制）+ 本地 MD/HTML**；小红书长图留 P2。
4. **模板默认数量/绑定**：✅ 默认 1 全局、可多套、不绑账号、关联使用（同配图风格）。
5. **模型选择**：✅ 用全局默认 LLM，本模块不做逐任务模型选择（轻任务）。
6. **（实现核对项，非阻塞）md2wechat 自定义 CSS 入口形态**：工程联调时确认其"主题 + 可选 customCss 覆盖"接口，PRD 口径不变。

---

## 10. 与上游模块的输入契约（对齐引用）

- **成稿**：Markdown（模块五 §4.4），`<评审意见>` 改稿已并入（模块八）。排版消费最终成稿。
- **配图资产 + 配图点**：模块九~十一（§4.1/4.3/4.4）。正文图 path + 封面 path（2.35:1）经配图点关联带入；排版 AI 重新推导落位。
- **账号定位**：模块一 `<账号定位>` XML，可选，用于模板推荐 / 导读基调。
- **网关 extractBlock**：`技术规格-模型网关.md` §10.1，本模块 `<排版方案>` 统一走此截取。

> 至此，**模块一~十二全部封板**，自媒体内容生产流水线（账号定位→热点→选题→框架→写文章→素材→评审→配图→排版）需求侧完全闭环。

*下一步*：进入工程实现（模型网关 MVP → 模块一联调 → 按流程顺序逐模块落地；渲染内核 md2wechat 先行集成验证）。
