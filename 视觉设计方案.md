# 墨流 · 视觉设计方案（Design System v0.1）

> 配套：`UI-原型.html`（可交互高保真原型，含亮/暗主题切换）
> 适用：Windows 本地桌面应用 · 自媒体内容生产流水线
> 原则：设计系统先行（组件先于页面）、无障碍 WCAG AA 基线、双主题、本地私有气质

---

## 一、设计定位（Why）

产品是「本地私有、账号驱动、每环节可人工干预」的内容生产流水线。界面需要同时传达两种气质：

- **可信 / 专业**（本地存储、Key 不出本机、可溯源）—— 用克制的中性灰底、清晰层级。
- **创造感 / AI**（账号定位→成稿→配图→排版）—— 用靛蓝/紫主色暗示"AI + 创意"，不喧宾夺主。

**一句话**：冷静的专业工具，带一点创造的灵气。不模仿花哨的运营后台，也不做成纯代码编辑器。

---

## 二、设计令牌（Tokens）

### 2.1 色彩

| 类别 | Token | 亮色 | 暗色 |
|------|-------|------|------|
| 品牌主色 | `--brand-500` | `#4F46E5`（靛蓝） | `#6366F1` |
| 品牌渐变 | `--brand-grad` | `135deg #6366F1→#8B5CF6` | 同 |
| 成功/完成 | `--ok` | `#10B981` | 同 |
| 警告/中危 | `--warn` | `#F59E0B` | 同 |
| 错误/高危 | `--err` | `#EF4444` | 同 |
| 信息 | `--info` | `#3B82F6` | 同 |
| 应用背景 | `--bg` | `#F6F7F9` | `#0E1015` |
| 卡片表面 | `--surface` | `#FFFFFF` | `#171B24` |
| 边框 | `--border` | `#E7E9EE` | `#272D3A` |
| 主文字 | `--text` | `#1E2230` | `#E6E8EC` |
| 次文字 | `--text-2` | `#5B6472` | `#A2A9B6` |
| 弱文字 | `--text-3` | `#9AA1AE` | `#6E7686` |

**语义映射**：流水线阶段状态用 绿（已完成）/ 靛蓝（进行中）/ 灰（未开始）；评审严重度用 红（高）/ 橙（中）/ 灰（低），与全局令牌一致，不另起调色板。

**无障碍**：正文文字与背景对比 ≥ 4.5:1（WCAG AA）；大字 ≥ 3:1；聚焦态用 2px 主色描边 + 2px 偏移。

### 2.2 字体

- 中文 UI / 正文：`"PingFang SC","Microsoft YaHei","Hiragino Sans GB", system-ui`
- 等宽（代码块 / 提示词 / XML）：`"JetBrains Mono","Fira Code", Consolas`
- 字号阶梯：12 / 13 / 14 / 16 / 18 / 20 / 24 / 30 px（4px 节奏衍生）
- 字重：400 常规 / 500 中 / 600 半粗 / 700 标题

### 2.3 间距 / 圆角 / 阴影

- 间距基准 4px：`4 / 8 / 12 / 16 / 20 / 24 / 32 / 40`
- 圆角：`6 / 10 / 14 / 20`（按钮 6–10，卡片 10–14，大容器 20）
- 阴影：sm（卡片静态）/ md（hover 浮起）/ lg（弹层）；暗色下阴影加深为半透明黑。

---

## 三、布局范式（Layout）

桌面应用标准三段式：

```
┌────────────┬───────────────────────────────────────┐
│  侧边栏     │  顶栏（标题 / 面包屑 / 账号芯片 / 主题切换） │
│ 232px      ├───────────────────────────────────────┤
│ 导航 +      │                                       │
│ 模型状态    │   主内容区（可滚动）                    │
│            │   - 总览：KPI + 9 阶段卡片网格 + 账号/动态 │
│            │   - 工作台：左上下文 / 中主画布 / 右 AI 动作 │
└────────────┴───────────────────────────────────────┘
```

**工作台统一三栏**：左（上下文：框架/素材/账号/资产）· 中（主编辑或渲染画布）· 右（AI 动作 + 版本历史 + 评审意见）。所有 12 个模块共用此骨架，仅中间区内容随阶段变——降低认知负荷与开发成本。

**栅格**：内容区最大宽度自适应，卡片用 12 列弹性网格；总览阶段卡 3 列，资产缩略图 2 列。

**响应式**：桌面优先（本应用为 Windows 桌面）；后续若出 Web 预览，断点 640 / 768 / 1024 / 1280 复用同一令牌。

---

## 四、核心组件库（Component）

| 组件 | 形态 | 状态 |
|------|------|------|
| 按钮 `.btn` | primary（渐变实心）/ ghost（描边） | default / hover（浮起）/ disabled |
| 导航项 `.nav-item` | 图标 + 名 + 阶段号徽标 | default / hover / active（主色浅底） |
| 卡片 `.card` / `.stage` | 表面 + 边框 + 左侧状态条 | default / hover（浮起+主色边） |
| 状态徽标 `.badge` | done（绿）/ active（靛蓝）/ todo（灰） | — |
| 输入 / 下拉 / 文本域 | 浅底 + 聚焦主色环 | default / focus / error |
| 芯片 `.chip` | 标签 / 筛选 | brand / neutral |
| 版本条目 `.ver` | 版本名 + 时间，当前态高亮 | current / normal |
| 评审意见 `.opinion` | 左色条 + 严重度徽 + 角色 | high/mid/low |
| 渲染预览 `.render` | 套用排版模板后的成品（白底，独立于主题） | — |

**一致性规则**：所有交互元素圆角 ≤10px、聚焦必有可见描边、hover 必有反馈（颜色或位移）、图标统一 18px 网格。

---

## 五、主题策略（Theming）

- 默认 **亮色**（贴合用户当前环境、公众号编辑使用习惯）。
- 一键切换 **暗色**（右上胶囊控件），仅切换 CSS 变量，不改结构。
- **渲染预览区强制白底**（`.render` 固定 `#fff`），因为成品最终粘进公众号后台，预览须贴近真实发布效果——这是有意的"预览即所见"。

---

## 六、原型覆盖的界面（看效果用）

`UI-原型.html` 已可实现：

1. **流水线总览**：KPI 条 + 9 阶段状态卡片（含已完成/进行中态）+ 当前账号卡 + 最近动态。点阶段卡可进对应工作台。
2. **写文章工作台**：左框架/素材/账号 · 中 WYSIWYG 编辑器（含标题/引用/代码块/配图占位）· 右 AI 写改面板 + 版本历史 + 评审意见。
3. **排版工作台**：左成稿+配图资产+模板选择 · 中 排版方案（可编辑插入点/导读/互动语）+ md2wechat 渲染预览 · 右 导出（复制到公众号 / MD / HTML）。
4. **亮/暗主题切换**、**导航切换**、**渲染按钮骨架演示**。

其余模块（账号/热点/选题/框架/素材/评审/配图）用同一设计系统占位，可直接按各自 PRD 套用，无需重新定义视觉语言。

---

## 七、工程交接注意

- 所有视觉值以 CSS 变量（令牌）形式落地，禁止硬编码色值。
- 组件库建议用同一套（如 Tauri + 原生 Web 视图 / Electron），样式与令牌直接复用本原型。
- 图标建议统一 18px 线性图标集（Lucide / Material Symbols）。
- 渲染输出（md2wechat）的样式独立于应用主题，由排版模板 CSS 控制，见 `PRD-排版AI.md` §4.2 / §4.3。

> 下一步：原型确认方向后，可逐个模块出高保真设计稿 + 标注（间距/字号/状态），进入开发。
