# PRD · 框架 AI（模块四 / 业务环节 5）

> 文档状态：Draft v0.2 ｜ 范围：单模块深度定义 ｜ 平台：Windows 本地桌面应用（无云端）
> 关联：上游可选消费「账号定位」（模块一）+「选题」（模块三）+「素材」（模块六，已接入）；下游供「写文章 AI」（模块五）。模型调用统一走 `技术规格-模型网关.md`。
> 平台约束：Windows 本地应用，数据本地存储，API Key 不出本机。
>
> **v0.2 关键决策（本轮拍板）**：
> 1. **框架模板（可编辑）**：默认模板 = `标题 / 开头 / 论点一 / 论点二 / 论点三 / 结尾`；用户可增删 section、改名、重排顺序、保存自定义模板并设默认。结构由模板驱动（取代原"论点数 N 指定"）。
> 2. **卡片墙呈现**：MVP 不用列表（看不清），改为**一行 4 个卡片、纵向滚动、最新生成置顶**，与选题卡片墙同源交互。
> 3. **`<素材>` 内部结构**：仅注入用户已选网页素材的 `Summary` 与手工文字素材的摘要；来源与链接仅作溯源。图片素材只作视觉参考，不能作为正文事实依据。
> 4. **支撑要点上限**：每个论点 2–4 条（提示词约束，不自适应）。
> （v0.1 已拍板、继续有效的：无素材模式 / 提纲式骨架 / XML 包裹纯文本 三条核心约定不变。）

---

## 1. 背景与 Why

框架是整个流水线的**结构层**——把"选题（决策）"翻译成"文章怎么搭"。它回答：标题写什么、要不要开头、分几个论点、每个论点的核心句与支撑要点、怎么收尾。

关键边界（用户拍板）：**框架是骨架，不是成稿**。框架里每段是提纲式（核心句 + 支撑要点），不扩写成完整段落——否则与写文章 AI 职责重叠，且框架会变重、下游难对齐。

松耦合原则延续：框架不强制依赖上游。可关联一个已存在的选题（推荐），也可手动起（用户手填主题/核心观点）；可带账号定位，也可不带。关联是软引用，可后补。

**素材处理（已落地）**：框架始终支持无素材模式；需要时，用户从可复用素材集合多选网页 `Summary` 或手工文字摘要注入 `<素材>`。不抓取网页全文，图片也不进入事实推理上下文。

**本轮形态升级（v0.2）**：框架不再用"固定结构 + 指定论点数"，改为**模板驱动**——系统内置默认模板（标题/开头/论点一/二/三/结尾），用户可编辑模板（增删 section、改名、重排），并可保存多个自定义模板。AI 按所选模板的 section 列表填充内容。这比"固定 N 论点"更贴合公众号实际写作（普遍有"开头钩子 + 三论点 + 收尾"范式）。

---

## 2. 目标（Goals）

- **骨架清晰**：按模板产出 标题 + 开头 + 论点×N + 结尾，每段核心句 + 支撑要点，可直接指导写文章。
- **边界分明**：只搭骨架，不写成稿（Non-goal）。
- **模板可塑**：默认模板开箱即用，用户可编辑/保存自定义模板，适配不同栏目风格。
- **格式统一**：XML 包裹纯文本（`<框架>`），section 名由模板决定，下游写文章 AI 泛型解析。
- **解耦可选**：选题/账号/素材均为可选软引用；支持无素材、无选题（手动）模式。
- **可干预**：生成即草稿，section 可改、可手增删、可换模型重生成、版本历史。
- **可串接**：经关联字段（topicId/accountIds/materialIds）与上下游软连接。

---

## 3. 用户故事（User Stories）

- 作为**运营者**，我希望关联一个选题 + 账号，让 AI 按"标题/开头/论点/结尾"模板直接搭出骨架，而不是从空白写。
- 作为**运营者**，我希望骨架里每段只是"核心句+支撑要点"的提纲，扩写留给写文章环节，别一次写太满。
- 作为**运营者**，我希望框架能用预设模板（开头钩子 + 三论点 + 收尾），也让我改模板（比如加"金句"段、删掉开头），适配不同号。
- 作为**运营者**，我希望框架是 XML 纯文本，写文章 AI 能直接拿去用，不用我再复制粘贴。
- 作为**运营者**，我希望素材模块做好前也能先用框架（无素材模式），不被卡住。
- 作为**运营者**，我希望生成的多个框架以卡片呈现（一行 4 个），一眼对比，别挤在列表里看不清。

---

## 4. 功能需求

### 4.1 创建入口（松耦合，两种）

- **A. 关联选题（推荐）**：从选题库选一个选题 → 自动带入 `<选题>`（序列化其 JSON）+ 该选题关联的 `accountIds` 与 `relatedHotIds`（溯源）。
- **B. 手动起**：用户手填主题/核心观点等（或仅主题）→ 不关联选题，`topicId` 可空。

> 两种入口都收敛为同一组输入：`<账号定位>`（可选）+ `<选题>`（可选）+ `<素材>`（可选，当前为空）+ **所选模板**。不强关联。

### 4.2 框架模板（可编辑，v0.2 核心）

**默认模板（系统预置，可改）** —— section 顺序即文章结构：

| 顺序 | Section 名 | 含义 | 是否必填 |
|------|-----------|------|---------|
| 1 | 标题 | 主标题（可选副标题） | 必填 |
| 2 | 开头 | 钩子/引入，建立共鸣或抛出问题 | 必填 |
| 3 | 论点一 | 核心论点 1（核心句 + 支撑要点） | 必填 |
| 4 | 论点二 | 核心论点 2 | 必填 |
| 5 | 论点三 | 核心论点 3 | 必填 |
| 6 | 结尾 | 收束 + 升华 | 必填 |

**模板编辑能力（用户）**：
- 增删 section（如加"金句段""数据支撑段"，删"开头"）。
- 改名 section（如"论点一"→"痛点剖析"）。
- 重排顺序（拖拽）。
- 保存为**自定义模板**（命名），可设其一为**默认模板**；支持"恢复系统默认"。
- 模板存储在本地设置（与账号定位/选题的字段 schema 同层，均为"可配置结构定义"）。

**生成时**：用户选择模板（默认用当前默认模板）→ AI 按该模板的 section 列表逐段填充。模板决定 `<框架>` 的 section 构成，**不传"论点数 N"**。

### 4.3 AI 输入（三要素 + 模板，素材可选）

```
[账号定位] = <账号定位>…</账号定位>      （可选，accountIds 为空时不带）
[选题]     = <选题>…</选题>              （可选，topicId 为空时不带；序列化选题 JSON）
[素材]     = <素材>…</素材>              （可选，**当前 P1 未接入，为空**）
[模板]     = 所选模板的 section 列表（如 标题/开头/论点一/二/三/结尾）
```

提示词示意（伪代码）：
```
你是公众号文章结构策划。基于以下信息，按【模板结构】搭文章骨架（只给提纲，不写正文成稿）。

<账号定位> {账号名称：… 领域：… 写作风格：… IP人设：…} </账号定位>

<选题>
{选题主题：… 切入角度：… 目标读者：… 核心观点：… 情绪基调：… 拟标题方向：…}
</选题>

【模板结构】请严格按以下 section 顺序输出，每个论点含「核心句」+ 2–4 条「支撑要点」（提纲式，不展开成段）：
- 标题
- 开头
- 论点一
- 论点二
- 论点三
- 结尾

严格按 <框架> 标签包裹纯文本输出（section 名即上面列表项）。
<框架> 标题：… 开头：… 论点一：核心句：… 支撑要点：- … 论点二：… 论点三：… 结尾：… </框架>
```

### 4.4 AI 输出（XML 包裹纯文本，提纲式，模板驱动）

**格式（标签 `<框架>`，section 由所选模板决定）**
```
<框架>
关联账号：量子观察者
关联选题：大模型迭代加速，普通人该如何自处
标题：当大模型再次进化，普通人该慌吗？
开头：
  核心句：上周同事被"AI 替代"的传闻刷屏，我反问了一句——被替代的，真的是人吗？
  支撑要点：
    - 钩子：用身边真实焦虑切入
    - 转折预告：下文给方法论而非鸡汤
论点一：
  核心句：AI 不是来抢饭碗，是来放大普通人的杠杆。
  支撑要点：
    - 历史规律：每次生产力革命短期取代、长期创造新岗位
    - 真实案例：用 AI 提效的职场人样本（待素材补充）
    - 反常识：最该怕的不是 AI，是不会用 AI 的人
论点二：
  核心句：焦虑的解药是"人机协作"的具体方法论。
  支撑要点：
    - 三步法：界定任务→拆解→让 AI 做脏活
    - 避坑：别把判断权全交出去
论点三：
  核心句：普通人的护城河，是"定义问题"的能力。
  支撑要点：
    - 对比：AI 擅长解已定义的问题
    - 行动：把精力放在提问而非答题
结尾：
  核心句：工具会迭代，人的价值在"定义问题"。
  升华句：与其担心被替代，不如成为驾驭工具的那批人。
</框架>
```

> **提纲式定义**：每段「支撑要点」是纯文本多行要点（非成稿段落），2–4 条。框架**不输出完整正文段落**——这是写文章 AI 的输入，不是框架的输出。

**内部存储**：框架以 XML 纯文本（`rawXml`）存储。编辑视图解析各 section 到表单，保存时回填 XML 纯文本。下游写文章 AI 直接拿 `rawXml` 当上下文（与全局约定一致，无 JSON 转换）。

> **泛型消费提示（重要）**：因模板可编辑、section 名不固定，`<框架>` 的 section 集合是动态的。下游**写文章 AI（模块五）必须按 section 名泛型解析**（遍历 `<框架>` 内所有 section），不能假设固定"三论点"。这点需在模块五 PRD 显式约束。

### 4.5 生成数量（候选）

- 支持一次生成 **count 个框架候选**（默认 1，可选 1–3），即 **count 次独立 AI 调用**，每次产 1 个 `<框架>`。
- 调用间相互独立（解耦），单任务失败不影响其他。
- 框架比选题"重"，默认 1 个；多候选用于对比不同结构/角度。

### 4.6 呈现与操作（卡片墙，v0.2）

- **卡片墙**：**一行 4 个卡片**，纵向滚动，**最新生成置顶**（与选题卡片墙同源交互，区别于模块二的"每行 4 平台热榜"——此处是框架产物卡片）。
- **卡片内容（默认折叠摘要）**：
  - 卡头：标题（主）+ 关联标记（选题名 / 账号名，可空则不显示）。
  - 卡身：列出模板各 section 名 + 其核心句首行（截断，约 1 行）；点击/展开看完整 `<框架>` 纯文本或结构化表单。
  - count>1 时直接渲染多张卡片，无需并排对比视图。
- **操作**：查看 / 编辑（改 section 核心句、支撑要点，可手增删 section）/ **锁定** / 删除 / 关联写文章。
- 生成即进"我的框架"卡片墙（无需单独"收藏"动作），供写文章 AI 关联消费。

### 4.7 人机协同（HITL）与状态机（放宽口径）

- **草稿态**：生成后即草稿，所有 section 可编辑，可手增删 section、改结构。
- **锁定**：点"锁定"→ `locked`（定稿基线）；但**非下游硬门槛**（已确认放宽）——写文章 AI 可消费草稿态框架。
- **换模型/重生成**：生成新版本（新 `rawXml`），旧版本进历史可回滚，不丢手动编辑；重生成复用同一输入（选题/账号/素材/模板）。
- **关联可后补**：生成时未关联选题/账号，事后可在框架上补 `topicId`/`accountIds`。

**与上下游的关联（非耦合）**
```
[账号定位]──┐
[选题]──────┼──► 框架（独立实体，可无关联独立存在，按模板搭骨架）
[素材 P1]──┘       │ 关联(topicId/accountIds/materialIds)
                  ▼
            写文章 AI（模块五，按关联消费 <框架> + <账号定位> + <素材>，泛型解析 section）
```

---

## 5. 前置依赖

- **模型网关**（见 `技术规格-模型网关.md`）：框架 AI 经 `gateway.chat()` 调用（支持纯文本输出，无需 json_mode）。
- **账号定位模块**（模块一，已封板）：可选 `<账号定位>` 上下文。
- **选题模块**（模块三，已封板）：可选 `<选题>` 关联（序列化其 JSON）。
- **素材模块**（P1，未做）：`<素材>` 当前为空；P1 接入后框架输入自动包含，无需改框架逻辑。
- **框架模板**：存储在本地"结构模板"设置区（与账号字段 schema、选题字段 schema 同层的可配置定义）。

---

## 6. 成功指标（Success Metrics）

| 指标 | 类型 | 目标 |
|------|------|------|
| 框架生成成功率 | 健康 | ≥ 95% |
| XML 结构合规率（可解析为模板 section） | 健康 | ≥ 98% |
| 用户锁定率（生成→锁定进写文章） | 驱动 | 观察骨架质量 |
| 无素材模式可用性（素材为空时正常出骨架） | 健康 | 100% |
| 模板可编辑可用性（增删改/保存/设默认） | 健康 | 用户自定义模板可正常驱动生成 |
| 生成耗时（P95，单框架） | 健康 | < 20s |

---

## 7. 风险与缓解

| 风险 | 影响 | 缓解 |
|------|------|------|
| 无素材时支撑要点空洞 | 骨架缺实质内容 | 提示词要求"标注待素材补充处"；P1 素材接入后改善；风险在 PRD 明示 |
| XML 解析失败 | 编辑/下游消费崩 | 解析兜底 + 失败标记"格式异常"可手动编辑 |
| 框架写成成稿（越界） | 与写文章重叠 | 提示词强约束"只提纲不写正文"；Non-goal 明确 |
| 模板编辑致 section 名飘 | 下游写文章 AI 解析错位 | `<框架>` section 由模板驱动；**模块五 必须泛型解析**；风险在评审标注 |
| 素材接入后格式错位 | `<素材>` 内部结构未定 | P1 时定义 `<素材>` schema，框架仅透传不解析 |
| 锁定的框架被下游用草稿 | 质量波动 | 下游消费时提示"该框架为草稿态" |

---

## 8. 验收标准（Acceptance Criteria）

1. 用户可从**两种入口**（关联选题 / 手动）创建框架，关联字段均可空；收敛为 `<账号定位>`+`<选题>`+`<素材>`+模板 输入。
2. 系统内置**默认模板**（标题/开头/论点一/二/三/结尾）；用户可**增删 section、改名、重排、保存自定义模板并设默认**。
3. 框架 AI 按所选模板的 section 列表输出 **`<框架>` XML 纯文本**，每段（标题/开头/各论点/结尾）= 核心句 + 2–4 条支撑要点。
4. 输出为**提纲式**（支撑要点非成稿段落）；**不含完整正文**。
5. **无素材模式**：`<素材>` 为空时框架正常生成（P0 不被 P1 阻塞）。
6. 支持生成 **count 个候选**（默认 1），多次独立调用。
7. 框架以**卡片墙**呈现：**一行 4 个、纵向滚动、最新置顶**；支持 编辑 / 手增删 section / 锁定 / 删除 / 关联写文章。
8. 下游（写文章 AI）可经关联消费草稿态或锁定态框架（锁定非硬门槛），并**按 section 泛型解析**。

---

## 9. 开放问题（旧 4 项均已本轮拍板，无新增阻塞项）

1. ~~`<素材>` 内部结构~~ → **P1 接入时定**，框架只透传不解析。（已决）
2. ~~框架模板~~ → **预设默认模板（标题/开头/论点一/二/三/结尾）+ 支持编辑/保存自定义模板**。（已决）
3. ~~列表 vs 卡片~~ → **卡片墙，一行 4 个**。（已决）
4. ~~支撑要点上限~~ → **2–4 条/论点**。（已决）

> 遗留关联事项（非本模块，提醒模块五）：因模板可编辑，`<框架>` section 名动态，写文章 AI 须泛型解析——将在 `PRD-写文章AI.md` 显式约束。

---

*下一步*：框架模块封板 → 模块五：写文章 AI（消费 `<框架>` + `<账号定位>` + `<素材>`，按 section 泛型解析、扩写成稿，支持改稿；须兼容框架模板的可变结构）。
