# PRD · 账号定位 AI（模块一 / 业务环节 1）

> 文档状态：Draft v0.3 ｜ 范围：单模块深度定义 ｜ 平台：Windows 本地桌面应用（无云端）
> 变更：v0.2 → v0.3 依据用户决策——LLM 输入格式由「XML 包裹 JSON」改为「**XML 包裹纯文本**」（JSON 过复杂）；静态向导 7 问顺序确认；自定义字段纯文本；锁定后**允许换模型重新生成**（留版本）。各模块内层格式各自约定，外层 XML 包裹全链路统一。

---

## 1. 背景与 Why

账号定位是整条内容生产流水线的**源头**，也是**所有下游 AI 调用的统一输入格式**。现实痛点：运营者往往说不清自己账号的定位，导致内容风格漂移，AI 生成的文章"不像这个号"。

本模块让用户用极少输入快速生成一套结构化、可复用、可锁定的账号定位，并允许人工微调。它同时定义全局"AI 生成 → 人工编辑 → 锁定 → 下游消费"模式（HITL pattern），后续 7 个环节直接复用。

**非目标（Non-goals）**：
- 不做多账号批量定位（本模块只做单账号创建/编辑）。
- 不做账号数据看板、粉丝画像分析（暂无平台数据接入）。
- 不绑定真实平台账号（只是本地"账号卡片"，非 OAuth 登录平台）。
- 不做定位的 A/B 测试或效果归因。
- **不做预制模板**（用户自行增删字段，预设到此为止）。
- **不做 Embedding 向量化**（账号↔热点匹配走提示词，见 4.4）。

---

## 2. 目标（Goals）

- **快**：用户数分钟内完成一个账号定位配置。
- **全**：默认字段覆盖下游所需维度，且用户可自由扩展。
- **可消费**：以 XML 包裹的纯文本落库，下游直接作为 LLM 上下文。
- **可控**：字段可增删改、可锁定、可换模型重新生成。

---

## 3. 用户故事（User Stories）

- 作为**自媒体运营者**，我希望用几个引导问题让 AI 帮我把账号定位补全，以便下游热点筛选和写稿保持一致风格。
- 作为**运营者**，我希望能添加 / 修改 / 删除任意字段（含自定义字段），以便精准表达我的账号。
- 作为**运营者**，我希望锁定后下游才能使用，以便避免中途改动导致已生成内容不一致。
- 作为**运营者**，我希望锁定后还能换模型重新生成，以便对比不同模型的效果而不丢手动编辑。

---

## 4. 功能需求

### 4.1 账号定位的表示：XML 包裹纯文本（全局约定）

账号定位在系统内部以**字段记录**存储（字段名 + 值，便于表单增删改），在与 LLM 交互时统一序列化为 **XML 包裹的纯文本**——外层 XML 标签，标签名即实体名；内层为「字段名：值」的纯文本（每字段一段，值可换行）。**不使用 JSON**（已决策：JSON 对这类内容过复杂）。

```xml
<账号定位>
账号名称：量子观察者
简介：用大白话讲清楚前沿科技
领域：科技/科普
目标受众：对科技感兴趣但非专业的成年人
写作风格：轻松幽默、类比多、少用术语
IP人设：陪你一起好奇的朋友
差异化定位：不追热点堆术语，而是把复杂事讲成人话
价值主张：关注我，每天搞懂一个科技冷知识
</账号定位>
```

**全局约定（已决策）**：
- **外层统一**：所有喂给 LLM 的实体都用「XML 标签包裹」，标签名即实体名（如 `<账号定位>`/`<选题>`/`<框架>`…）。
- **内层各自约定**：每个模块自行定义内层纯文本结构。本次只定**账号定位**的内层（「字段名：值」纯文本）；选题/框架/文章等留给各自模块 PRD。
- 内层一律**纯文本**，不用 JSON。

**内容字段（8 个默认 + 自定义，值均为纯文本）：**

| 字段 | 必填 | 说明 | 下游用途 |
|------|:---:|------|---------|
| 账号名称 | ✅ | 展示标识 | 全链路 |
| 简介 | ✅ | 一句话定位 | 写文章基调 |
| 领域 | ✅ | 垂直赛道 | 热点筛选、选题 |
| 目标受众 | ✅ | 受众画像 | 选题、文章语气 |
| 写作风格 | ✅ | 语气调性 | 写文章 AI |
| IP人设 | ✅ | 角色设定 | 写文章、配图 |
| 差异化定位 | ✅ | 与同类不同 | 选题角度 |
| 价值主张 | ✅ | 关注能得到什么 | 文章价值锚点 |
| （自定义字段） | 可选 | 用户自行添加，纯文本 | 随用 |

> 所有字段值均为**纯文本**（已决策）。自定义字段为一等公民，同样以「字段名：值」进入同一段纯文本。

**系统字段（不进入 XML payload，仅 DB / 流水线元数据）**：状态（草稿 / 已锁定）、更新时间、创建时间、ID。这些控制流水线，不喂给 LLM。

> embedding 向量字段**已取消**（不做向量化）；账号↔热点匹配改由提示词完成（见 4.4）。

**实现提示**：AI 生成时内部可用 JSON mode / function calling 保证字段可靠解析（这是实现细节，不外暴露）；解析为字段记录后，对外/下游统一序列化为「XML 包裹纯文本」。

### 4.2 静态引导向导 + 账号定位 AI

- **引导形态（已决策：静态向导，问题与顺序已确认）**：固定 7 问，非 AI 追问：
  1. 这个账号叫什么？
  2. 主要做哪个领域？
  3. 写给谁看（受众）？
  4. 希望是什么写作风格 / 语气？
  5. 你（或这个号）扮演什么 IP 角色？
  6. 和同类账号比，你有什么不同？
  7. 关注你能给读者带来什么？

  （向导末尾允许用户附一段自由描述，补充上下文。）

- **账号定位 AI**：把向导答案 + 自由描述送给 LLM，生成包含全部 8 字段（可适度补全推断）的账号定位。temperature 偏低保稳定。
- **输出**：账号定位字段记录（对应 4.1 内容字段），存库后可序列化为 XML 包裹纯文本供下游消费。

### 4.3 交互流程（HITL pattern，全局复用）

```
静态向导（7 问，已定）→ [账号定位 AI 生成] → 表单草稿（字段记录）
       ↓
用户编辑：增 / 删 / 改任意字段（含自定义字段）
       ↓
确认无误 → 锁定（状态=已锁定）→ 下游可读（XML 包裹纯文本作上下文）
       ↓
锁定后允许"换模型重新生成"（基于向导原始输入 或 当前字段）
       → 生成新版本，版本历史保留，可对比 / 回滚（不丢手动编辑）
```

- 锁定后修改需先解锁，改动留版本历史。
- **换模型重新生成**：用户可选不同模型重跑，结果作为新版本，原版本保留可回滚。
- 此模式为全局样板，后续环节复用。

### 4.4 账号↔热点匹配（提示词方式，无 Embedding）

热点筛选 AI 判断"某热点是否适合本账号"时，将本账号的 `<账号定位>…</账号定位>` XML 纯文本作为上下文拼入 prompt，由 LLM 直接判断契合度并给理由。**不依赖向量检索**。

---

## 5. 前置依赖：模型网关（Model Gateway）——必须第一个建

账号定位 AI 及全部 12 个 AI 角色都依赖统一模型调用层。需求方已明确兼容范围，设计如下：

- **统一范式 = OpenAI-compatible 接口**（`base_url` + `api_key` + `model` 可配置）。
  - 原生 OpenAI-compatible 的厂商直连即可：**Deepseek、Kimi（Moonshot）、智谱（GLM OpenAI 模式）、豆包（火山方舟 Ark）、ChatGPT（官方）**。
  - **第三方中转站**（New API / One API 等）：用户把 `base_url` 指向中转站，中转站背后聚合 Claude/Gemini/上述所有，统一以 OpenAI 协议暴露——**一个抽象吃遍所有**。
  - **Claude / Gemini 原生直连**（不用中转站时）：作为可选适配器（Anthropic SDK / Gemini SDK），或通过厂商提供的 OpenAI-compat 桥接。
- **本地安全存储**：API Key 存于本地安全区（Windows Credential Manager / 本地加密文件），不上传、不硬编码。
- **能力协商**：统一抽象 `chat / vision / json_mode / streaming`，各厂商差异在适配器内抹平；不支持的能力在 UI 层禁用。
- **职责边界**：网关只负责传输与格式归一；XML 包裹纯文本是上层格式约定，与网关无关。

---

## 6. 成功指标（Success Metrics）

| 指标 | 类型 | 目标 |
|------|------|------|
| 定位生成耗时（P95） | 领先 | < 15s |
| 字段被人工修改占比 | 领先（AI 准确度） | < 40% 字段被改 |
| 生成后锁定使用率 | 滞后 | > 80% |
| 模型切换成功率 | 健康 | 100%（所有兼容模型可生成） |
| 换模型重生成使用率 | 洞察 | 观察用户对比意愿 |
| 自定义字段使用率 | 洞察 | 观察用户扩展意愿 |

---

## 7. 风险与缓解

| 风险 | 影响 | 缓解 |
|------|------|------|
| AI 生成的风格/差异化不准 | 下游内容跑偏 | 人工编辑兜底 + 锁定前强提醒 |
| 静态向导问题覆盖不足 | 生成质量低 | 向导末尾允许自由补充描述 |
| 不同模型输出格式不稳 | 字段解析失败 | 内部 JSON mode 强约束 + 失败重试 |
| 本地 Key 泄露 | 安全风险 | 本地加密存储，不落明文日志 |
| 模型价格/速率波动 | 成本/体验 | 网关层超时、重试、降级 |
| 换模型重生成占用成本 | 成本 | 限制重生成次数 / 提示计费 |

---

## 8. 验收标准（Acceptance Criteria）

1. 给定向导答案，账号定位 AI 输出包含**全部 8 必填字段**的账号定位（内部结构化可解析）。
2. 用户可对**任意字段**（含增删自定义字段）编辑并保存为草稿。
3. 锁定后状态变更为"已锁定"，下游 mock 消费者能读到 **XML 包裹的纯文本**。
4. 切换**任一兼容模型**（豆包/智谱/Kimi/Deepseek/ChatGPT/Gemini/Claude + 一个中转站示例）均能完成生成。
5. 解锁 → 修改 → 再锁定 全链路留痕（版本历史可见）。
6. 锁定后可**换模型重新生成**，生成新版本且旧版本可回滚。

---

## 9. 确认与延后

**已确认（本模块封板）**：
- LLM 输入格式 = XML 包裹纯文本（不用 JSON）；外层 XML 全链路统一，内层各模块各自约定。
- 默认字段 8 个 + 自定义，全纯文本，可增删改。
- 静态向导 7 问及顺序按 4.2 所列。
- 锁定后允许换模型重新生成（留版本，不丢手动编辑）。
- 取消 embedding / 预制模板 / 雷区 / 参考标杆。

**延后到各自模块 PRD**：
- 选题 / 框架 / 文章等实体的 XML 标签名与内层纯文本结构。
- 自定义字段是否需要类型 / 校验（当前纯文本，后续可扩展）。

---

*下一步*：模型网关最小可用版规格 → 账号定位 AI 联调 → 模块二：选题 AI。
