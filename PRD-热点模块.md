# PRD · 热点模块（模块二 / 业务环节 2）

> 文档状态：Draft v0.3 ｜ 范围：单模块深度定义 ｜ 平台：Windows 本地桌面应用（无云端）
> 关联：上游依赖「账号定位」（模块一，已封板）；下游供「选题」（模块三）消费。模型调用统一走 `技术规格-模型网关.md`。数据源为开源项目 **DailyHotApi**（今日热榜聚合 API）。
> 平台约束：Windows 本地应用，数据源建议本地 Docker 部署 DailyHotApi，无外部服务端依赖。

---

## 1. 背景与 Why

热点是内容生产的"原料"。现实痛点：自媒体运营者每天要在微博/知乎/抖音/头条等十几个平台间来回切换找话题，效率低、易遗漏、且不知道哪些热点"适合我的号"。

本模块把"找热点 → 收热点 → 筛热点"三件事收进一个本地界面：
1. **抓取并呈现**各平台实时热榜（数据源：DailyHotApi 开源聚合 API，约 40+ 平台）。
2. **手动收藏**感兴趣的热点（源数据快照锁定，用户不可改原文），并支持**打标签**管理。
3. **热点筛选 AI**：把已锁定的账号定位作为上下文，用提示词判断"这个热点适不适合我的号"，输出契合度 + 理由 + 建议切入角度——不靠向量，靠 LLM 语义判断（与 `PRD-账号定位AI.md` 4.4 一致）。

**非目标（Non-goals）**
- 不修改热搜原文（源数据只读，与用户"热搜词不做修改"一致）。
- 不做热榜的二次编辑/改写/摘要生成（那是素材/框架阶段的事）。
- 不抓取需登录/付费的付费内容源。
- AI **不生成热点**，只筛选已有热点（不无中生有）。
- 不做热点趋势预测、历史回溯分析（DailyHotApi 有历史能力，本模块 MVP 不做）。
- 不做跨账号的"热点热度总榜"（收藏为单账号视角，跨账号聚合延后）。
- 筛选 AI **不叠加关键词过滤**（用户明确：仅按账号定位筛选）。
- 收藏**不按平台/状态分 Tab**，统一一个列表（用户明确）。

---

## 2. 目标（Goals）

- **全**：聚合多平台热榜，一处看全。
- **快**：一键刷新，秒级呈现。
- **准**：热点筛选 AI 关联账号定位，过滤掉不合适的，聚焦命中账号。
- **可溯源**：收藏热点锁定源数据 + 来源 URL，下游选题可回溯。
- **可管理**：收藏支持标签，单列表即可应对后续选题取用。
- **可消费**：收藏热点结构化为下游选题的"原料池"。

---

## 3. 用户故事（User Stories）

- 作为**自媒体运营者**，我希望在一个界面看到各平台实时热榜，以便不用来回切换找话题。
- 作为**运营者**，我希望把感兴趣的热点收藏起来，并给它打标签，以便后续按标签做选题。
- 作为**运营者**，我希望 AI 按我的账号定位筛掉不合适的热点，以便聚焦、不浪费时间。
- 作为**运营者**，我希望热榜原文不被改动，以便收藏的是"真实发生的事"，下游可溯源。
- 作为**运营者**，我希望筛选时只取热搜前 N 名喂给 AI，以便控制成本、提高效率。

---

## 4. 功能需求

### 4.1 热搜抓取与呈现（数据源：DailyHotApi）

**集成方式（v0.4 已确认）**
- 应用安装包内置 **DailyHotApi 2.0.8**，由 Electron 主进程托管，仅监听 `127.0.0.1` 随机端口；用户无需安装 Docker。
- 内置服务依赖、安全覆盖版本与许可证记录见 `docs/decisions/0004-embedded-dailyhot-api.md`，禁止使用 `latest`。
- 页面必须显示“DailyHotApi v2.0.8 / 草稿数据源 / 使用前核实来源”警告。
- 后续可增加**自建/公共实例地址**作为高级故障兜底，但不作为默认路径。
- 调用即 `GET {base}/{source}`，返回 `{ code, title, subtitle, updateTime, data:[{id,title,desc,pic,url,...}] }`。
- DailyHotApi 当前主版本约 **40+ 平台**（含微博、知乎、百度、抖音、今日头条、哔哩哔哩、稀土掘金、少数派、IT之家、腾讯新闻、网易新闻、虎扑等），具体以 `GET {base}/all` 返回的路由列表为准；应用内通过 `/all` 动态发现可用源。

**平台清单与默认显示**
- 应用从 DailyHotApi `/all` 拉取全部可用平台，**默认全量展示**（部署一次即可调用所有接口，无额外部署成本）。
- 用户可在设置里**隐藏 / 重新显示**任意平台；隐藏的平台不显示在热榜墙，但仍可在筛选 AI 时手动勾选。
- **可用性说明**：DailyHotApi 部分平台源站可能失效（🟠 可能失效 / ❌ 无法使用）或需科学上网（⚠️），该类平台在热榜墙中显示轻量错误态（空列 + 提示），不影响其他平台正常渲染。
- 默认全量展示会带来首屏列数较多、需横向滚动；如觉得干扰，用户可隐藏非主流平台（如只留 微博/知乎/抖音/头条/百度/B站 6 个）。**热榜墙"看全"与 AI 筛选"聚焦"是两个独立开关**：看全不影响，AI 筛选仍可按需勾选平台 + 取前 N 名控制成本。

**数据模型（热榜条目，源数据只读）**
```
HotItem {
  id: string;            // 平台内唯一 id
  title: string;         // 热点标题（只读）
  desc: string;          // 描述（只读，有的平台无）
  pic: string?;          // 缩略图（只读）
  url: string;           // 来源链接（溯源）
  source: string;        // 平台调用名，如 "zhihu"
  sourceTitle: string;   // 平台展示名，如 "知乎"
  subtitle: string;      // 榜单类型，如 "热榜" / "热搜榜" / "24h热文榜"
  updateTime: string;    // 抓取时间
  hotValue: string?;     // 热度值（如 "2454万热度"）
  rank: number;          // 在源榜单中的排名（1-based）
  rawJson: string;       // 原始响应快照（排障/扩展）
}
```

**呈现：多列卡片式热榜墙（参考你上传的布局）**
- 界面主体为**网格卡片墙**：每行最多 4 个平台卡片，纵向滚动向下排列（瀑布/网格布局），不是横向滚动。
- 每列卡片结构：
  - 列头：平台图标 + 平台名称 + 榜单类型（如"知乎 · 热榜"），右侧显示"刷新"按钮与最后更新时间。
  - 列身：排名列表，每条显示 排名序号 / 标题 / 热度值（若有）。
  - 行交互：hover 显示"收藏"按钮；点击标题可在系统浏览器打开原链接（可选，本地应用内不内嵌浏览器）。
- 列宽固定，行内 4 列等宽；窗口宽度不足时优先保证每行 4 列，整行缩放（不拆成 5 列或 3 列）。
- **栏目顺序可调整**：用户可拖拽平台卡片改变排列顺序，顺序保存到本地设置。
- 全局顶部工具栏："一键刷新全部"、"打开收藏列表"、"AI 筛选"、"设置显示平台"。
- 单平台刷新失败时，该列顶部显示轻量错误提示（如"暂时无法获取"），不影响其他列。
- **源数据只读**：标题/描述/热度不可编辑（与用户"热搜词不做修改"一致）。

**缓存与稳定性**
- DailyHotApi 自带缓存（默认 30min），应用层不额外长缓存；刷新即拉最新。
- 应用层对单平台失败**优雅降级**：某平台失效只提示该平台，不影响其他平台（DailyHotApi 免责声明：源可能随时失效）。

### 4.2 收藏（Favorites，锁定 + 标签）

- 用户可把任意 `HotItem` 加入收藏（本地表 `hot_favorites`）。
- **收藏即锁定**：存入源数据快照（title/desc/url/source/rank/hotValue 全量），**不可改**（与用户"热搜词不做修改"一致，且保证下游溯源真实）。
- 支持：单条收藏、批量收藏、取消收藏、收藏列表查看。
- **收藏列表为单一列表，不打散到 Tab**；通过**标签**进行组织：
  - 系统预置两个标签（可重命名/删除）：`待选题`、`已用`。
  - 用户可自定义标签，一条收藏可打多个标签。
  - 列表支持按标签过滤、按平台过滤、按时间排序。
- 收藏列表是**选题模块的原料池**（模块三消费）。
- 去重：同一 `source+id` 不重复收藏；若再次收藏给出"已存在"提示并跳转原条目。

**收藏数据模型**
```
HotFavorite {
  id: string;            // 本地 uuid
  hotItem: HotItem;      // 快照（锁定）
  tags: string[];        // 标签数组
  accountId: string?;    // 关联账号（可选，用于区分不同账号的收藏视角）
  createdAt: string;     // 收藏时间
  status: "active" | "archived";  // 可归档
}
```

### 4.3 热点筛选 AI（关联账号定位，提示词方式）

**触发**：用户选定一个**已锁定账号**（模块一产物），点"AI 筛选"，选择：
- 待筛范围：当前热榜墙可见平台 / 已收藏列表 / 指定平台（多选）。
- **取前 N 名**：每个平台取排名前 N 的热点进入筛选，默认 `N = 20`（用户已拍板），用户可在设置调整（建议范围 10–100）。

> 用户明确："筛选的时候可以筛选热搜前多少名，AI 一次性喂进去就可以，太多了反而效率低"。因此筛选 AI 单次消费"前 N 名"，不做全量无限喂入。

**输入（prompt 拼装，网关不感知）**
- 上下文：该账号的 `<账号定位>…</账号定位>` XML 纯文本（来自模块一）。
- 待筛热点：用 `<热搜列表>…</热搜列表>` XML 标签包裹，内部为"序号. [平台] 标题"的纯文本列表。

**示例 prompt 结构**
```
<账号定位>
账号名称：量子观察者
领域：科技/科普
目标受众：对科技感兴趣但非专业的成年人
写作风格：轻松幽默、类比多
...
</账号定位>

<热搜列表>
1. [知乎] 如何看待某大模型发布新版本，性能翻倍
2. [微博] 某明星离婚引发热议
3. [百度] 2026年菲尔兹奖公布
...
（共 50 条，来自各平台前 50 名）
</热搜列表>

请基于该账号定位，对 <热搜列表> 中的每条热点判断：
- 契合度：高 / 中 / 低（三档）
- 理由：一句话说明为什么契合/不契合该账号
- 建议切入角度：若契合，给出一个具体可写的切入点

请严格按以下格式输出，不要改写热点原文：
1. 契合度：高 | 理由：... | 切入角度：...
2. 契合度：低 | 理由：... | 切入角度：无
...
```

**行为**
- 把账号定位 + `<热搜列表>` 拼入 prompt，让 LLM 对每条热点判断契合度 / 理由 / 切入角度。
- 不依赖向量检索（与 `PRD-账号定位AI.md` 4.4 一致，纯提示词语义判断）。
- 调用 `gateway.chat()`，`temperature` 偏低（0.3）保稳定；优先使用 `json_mode` 若模型支持，否则用 prompt 强制格式 + 解析。
- **不叠加关键词过滤**（用户明确：筛选 AI 仅基于账号定位）。

**输出与交互**
- 返回筛选结果列表：每条热点带 `契合度 / 理由 / 切入角度`。
- 结果以**草稿态**呈现：用户可"采纳"（进收藏，默认打标签 `待选题`）/"忽略" /"重筛"。
- 支持按契合度过滤（仅看高 / 高+中）。
- 支持批量采纳：一键把所有"高"契合度热点加入收藏。

### 4.4 与上下游的状态机关系

```
热榜源数据（只读，不可改）
   │  用户收藏 / AI 筛选后采纳
   ▼
收藏热点（已锁定，不可改，可溯源，可打标签）
   │  下游：选题模块消费
   ▼
选题（模块三）

AI 筛选结果：草稿态 → 用户采纳才进收藏
```

- **状态机**：热榜源 = 只读；收藏 = 已锁定；筛选结果 = 草稿（需用户确认）。
- 下游（选题）只消费"已锁定"的收藏热点，符合全局"下游只消费锁定上游"原则（见总评审第二节）。

---

## 5. 前置依赖

- **模型网关**（见 `技术规格-模型网关.md`，P0 必做）：热点筛选 AI 经 `gateway.chat()` 调用。
- **账号定位模块**（模块一，已封板）：筛选 AI 必读其 `<账号定位>` XML 纯文本。
- **内置 DailyHotApi 2.0.8**：由 Electron 主进程托管，用户无需 Docker；升级策略见 ADR 0004。

---

## 6. 成功指标（Success Metrics）

| 指标 | 类型 | 目标 |
|------|------|------|
| 热榜抓取成功率（各平台） | 健康 | ≥ 90% 平台正常返回 |
| 热榜呈现耗时（P95，全平台刷新） | 领先 | < 8s |
| 收藏转化率（看到→收藏） | 洞察 | 观察用户筛选习惯 |
| 热点筛选 AI 采纳率（AI 建议→用户采纳） | 领先（AI 准确度） | > 50% |
| 筛选耗时（P95，50 条） | 健康 | < 20s |
| 锁定后溯源可用率 | 健康 | 100%（收藏含 URL/源） |

---

## 7. 风险与缓解

| 风险 | 影响 | 缓解 |
|------|------|------|
| DailyHotApi 部分源失效 | 某平台无数据 | 优雅降级，单平台提示，不影响其他；随项目更新扩展源 |
| 内置服务启动失败 / 上游接口变化 | 无法抓取或单源失败 | 启动健康检查；单平台降级；只随墨流版本升级并完成回归 |
| 热点筛选 AI 误判（漏掉好热点/误推不合适） | 选题质量 | AI 只建议，人工最终采纳；契合度三档+理由可复核 |
| 热榜内容版权 / 真实性 | 合规 | 收藏仅锁源数据+URL 溯源；DailyHotApi 免责声明提示用户自行核实 |
| 上游包体与依赖增加 | 安装包增大 / 安全面扩大 | 固定版本、保留许可证、生产依赖审计 |
| 筛选结果量大、token 成本高 | 成本 | 通过"取前 N 名"控制条数；只传 title/source 给 AI |

---

## 8. 验收标准（Acceptance Criteria）

1. 应用能拉取 DailyHotApi `/all` 中的平台列表，并默认以**网格卡片墙全量呈现**所有可用平台（每行 4 列、纵向滚动），含失效平台的错误态，每条含 排名/标题/热度值/来源。
2. 热榜条目**不可编辑**（只读），刷新可更新。
3. 收藏功能：单条/批量加入、取消、**单一列表查看**、**打标签**、按标签/平台过滤；同一热点不重复。
4. 收藏热点**锁定源数据**（title/desc/url/source/rank 不可改）。
5. 选定一个已锁定账号后，"AI 筛选"对**前 N 名热点**输出每条的 **契合度(高/中/低) + 理由 + 切入角度**。
6. 筛选 AI 输入严格为 `<账号定位>…</账号定位>` + `<热搜列表>…</热搜列表>` 两个 XML 块，不叠加关键词过滤。
7. 筛选结果草稿态，用户"采纳"的热点进入收藏（默认标签 `待选题`），且可被选题模块读取（预留接口）。
8. 某平台失效时，其他平台正常，且给出失效提示。

---

## 9. 决策记录与剩余开放问题

**已确认（v0.2 落定）**
- 平台：默认全量展示（基于 `/all` 动态发现），用户可隐藏非主流平台。
- 筛选 AI：仅基于账号定位，输入为 `<账号定位>` + `<热搜列表>` 两个 XML 块，不叠加关键词。
- 收藏：单一列表 + 标签；标签仅 `待选题`、`已用` 两个（用户明确，不加分类标签）。
- 筛选取前 N 名：默认 `N = 20`（用户拍板）。

**已确认（v0.3 最终落定）**
- 热榜墙布局：每行 4 个平台卡片，**纵向滚动**向下排列，**不是横向滚动**。
- 栏目顺序可拖拽调整，顺序保存到本地设置。
- 采纳后不跳转：留在筛选结果页继续处理，已采纳条目标记"已采纳"；支持"一键采纳全部高契合度"。

**已确认（v0.4）**
- DailyHotApi 打包进应用，用户无需 Docker；固定引用 2.0.8 并显示草稿警告。
- DailyHotApi 只随墨流正式版本升级，不独立更新、不启动时自动更新。

**已确认（v0.5）**
- 每次 AI 筛选可选择具体连接和模型，默认预选该连接的默认模型。
- 只允许已锁定账号；筛选结果保持草稿态，人工采纳后才进入锁定收藏。

**已确认（v0.6）**
- 平台隐藏/恢复与拖拽排序保存到本地；隐藏平台仍可参与 AI 筛选。
- 收藏列表支持按平台过滤，并可直接以收藏夹为范围发起 AI 筛选。

---

*下一步*：热点模块 MVP 落地（DailyHotApi 集成 + 热榜墙 + 收藏 + 筛选 AI）→ 模块三：选题 AI。
