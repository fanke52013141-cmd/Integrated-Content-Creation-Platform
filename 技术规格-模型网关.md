# 技术规格 · 模型网关（Model Gateway）

> 文档状态：Draft v0.2 ｜ 定位：前置基础设施规格（被 12 个 AI 角色 + 图像生成共享依赖）
> 变更：v0.1 → v0.2 依据用户决策——① **Claude/Gemini 原生直连 + 中转站两条路都做**（非仅中转站）；② **图像模型统一进网关**（新增 `generateImage()` 抽象与图像适配器层）；③ **移除成本单价表**（仅保留本地 token 用量/延迟记录供排障）；④ 超时/重试采用建议固定值（60s / 3 次）。
> 关联：本网关是 `PRD-账号定位AI.md` 第 5 节所述"模型网关"的正式落地；上层格式约定见 `产品方案评审-自媒体创作平台.md` 3.2 节（XML 包裹纯文本）。
> 平台约束：Windows 本地桌面应用（无服务端），模型调用跑在本地进程；API Key 不出本机。

---

## 1. 背景与 Why

整条流水线有 **12 个 AI 角色**（账号定位 / 热点筛选 / 选题 / 搜索拆解 / 素材浓缩 / 框架 / 写文章 / 评审×N / 配图策略 / 配图提示词 / 配图生成 / 排版），其中 LLM 类 11 个、图像类 1 个（配图生成 AI）。各家接口形态差异巨大：

- **OpenAI-compatible 派**：Deepseek、Kimi(Moonshot)、智谱(GLM)、豆包(火山方舟 Ark)、ChatGPT 官方 —— 协议近似，但 `base_url`、模型名、部分能力（vision / json_mode / function calling）不一致。
- **原生派**：Claude(Anthropic)、Gemini(Google) —— 协议独立，直连需各自 SDK。
- **中转站派**：New API / One API 等 —— 把上面所有聚合后以 OpenAI 协议统一暴露，用户只需一个 `base_url`。
- **图像派**：DALL·E(OpenAI) / Gemini Imagen / 即梦(字节 Doubao-Seedream) / Stable Diffusion(本地或 API) / Midjourney(无官方 API，需第三方代理) —— 协议比 LLM 更碎。

**痛点**：如果每个 AI 角色各自对接一个厂商 SDK，会出现"改一个厂商全员返工""新增一个模型要改 12 处"。**模型网关 = 单一抽象层**：业务层只认识 `chat()` 和 `generateImage()`，厂商差异全部沉到网关内部。

**用户已拍板的兼容范围**：国内（豆包 / 智谱清言 / Kimi / Deepseek）+ 国外（Gemini / ChatGPT / Claude）+ OpenAI 通用调用方式 + 第三方中转站（如 New API）。**原生直连与中转站两条路都做**。图像模型统一进网关。

---

## 2. 目标（Goals）/ 非目标（Non-goals）

**Goals**
- **单一调用入口（文本 + 图像）**：文本 `gateway.chat()`，图像 `gateway.generateImage()`，业务层不感知具体厂商。
- **OpenAI-compatible 为统一文本范式**：`base_url + api_key + model` 可配，覆盖绝大多数厂商与中转站。
- **原生适配器并行**：Claude / Gemini 原生直连与中转站聚合**都支持**，用户自选。
- **图像统一抽象**：`generateImage(prompt, opts)` 归一 DALL·E / Imagen / 即梦 / SD / Midjourney 代理。
- **零代码接入新模型**：新增一个模型 = 填 `base_url / key / model`，无需改业务代码。
- **本地安全存储 Key**：Key 存本地安全区，不落明文、不上传、不随配置导出。
- **能力协商**：统一 `chat / vision / json_mode / streaming / image` 能力描述，不支持的在调用层拦截。
- **健壮传输**：固定超时、重试、指数退避、可选降级 Provider。
- **统一响应块提取（抗 CoT）**：`chat()` 支持可选 `extractBlock` 参数，按标签名提取 `<标签>…</标签>` 之间的内容，泛型处理所有 XML 包裹输出的 AI 角色（账号定位/选题/框架/写文章/评审/配图…），避免每个角色各写一套解析。

**Non-goals（明确不做什么，防范围蔓延）**
- 不做模型微调 / 训练 / RAG 向量库（上层业务）。
- 不做多轮对话状态管理（业务层各 AI 角色自持上下文）。
- **不解析 XML 语义**（网关仍不理解 `<账号定位>` 等业务含义）。但提供**泛型文本后处理**：可选 `extractBlock` 按标签名（视作不透明字符串定界符）截取块，供各角色统一提取——这属于传输层工具，不构成业务语义理解。
- 不做中转站软件的部署/运维（网关只消费中转站，不提供中转站）。
- **不做成本单价表 / 花费估算**（用户已拍板：不要成本价格表）。仅本地记录 token 用量与延迟供排障。
- 不内置任何默认 Key（用户必须自己填，保障隐私）。

---

## 3. 整体架构

```
┌──────────────────────────────────────────────┐
│  业务层：12 个 AI 角色 + 配图生成              │
│  负责拼装 XML 包裹纯文本的 prompt，调用网关     │
└──────────┬─────────────────────────┬──────────┘
           │ gateway.chat()          │ gateway.generateImage()
           ▼                         ▼
┌──────────────────────────────────────────────┐
│              模型网关 Model Gateway             │
│                                                │
│  ┌─────────────┐  ┌─────────────────────────┐ │
│  │ 配置管理     │  │ 安全存储 KeyStore        │ │
│  │ ProviderCfg │  │ (Windows 本地加密)        │ │
│  └─────────────┘  └─────────────────────────┘ │
│                                                │
│  ┌──────── 文本适配层 Adapter ───────────────┐ │
│  │ • OpenAI-Compatible Adapter（通用，主）    │ │
│  │ • Anthropic Adapter（Claude 原生）         │ │
│  │ • Gemini Adapter（Gemini 原生）            │ │
│  └───────────────────────────────────────────┘ │
│  ┌──────── 图像适配层 Adapter ───────────────┐ │
│  │ • OpenAI Image Adapter（DALL·E）           │ │
│  │ • Gemini Image Adapter（Imagen）           │ │
│  │ • ByteDance Image Adapter（即梦）          │ │
│  │ • SD Adapter（本地/API）                   │ │
│  │ • MJ Proxy Adapter（Midjourney 第三方）    │ │
│  └───────────────────────────────────────────┘ │
│                                                │
│  ┌──────────────────────────────────────────┐ │
│  │ 能力协商 / 路由 / 超时 / 重试 / 降级 / 日志 │ │
│  └──────────────────────────────────────────┘ │
└───────────────────┬──────────────────────────┘
                    │ HTTPS
        ┌───────────┼───────────────────┐
        ▼           ▼                   ▼
   厂商官方API   中转站(New API…)    原生SDK / 图像API
```

**职责边界（最关键的一条）**：网关只负责"把统一请求翻译成厂商请求、把厂商响应翻译回统一响应、管 Key 与传输"。它**不知道也不关心**上游业务在 message 里塞了 `<账号定位>…</账号定位>` —— 那是一段纯文本，网关原样收发。图像同理：网关只传 prompt 与图片字节，不理解"配图策略"。

---

## 4. 核心抽象与接口设计

### 4.1 文本统一请求/响应（贴合 OpenAI `chat/completions` 形状）

**UnifiedRequest**
```ts
interface UnifiedRequest {
  model: string;          // 逻辑模型标识，如 "deepseek-chat" / "claude-3-5-sonnet" / "gpt-4o"
  messages: UnifiedMessage[];   // 纯文本或视觉多模态，网关不解析内容语义
  temperature?: number;   // 默认 0.7
  max_tokens?: number;
  json_mode?: boolean;    // 需要结构化输出时
  stream?: boolean;       // 流式
  extractBlock?: BlockExtraction;  // 可选：按标签名提取响应块（抗 CoT 噪声）
  // 以下由网关内部从 ProviderCfg 注入，业务层不传：
  //   base_url, api_key（来自 KeyStore）
}

interface UnifiedMessage {
  role: "system" | "user" | "assistant";
  content: string | UnifiedContentPart[];  // 纯文本；vision 时为 parts
}
```

**UnifiedResponse**
```ts
interface BlockExtraction {
  tag: string;              // 标签名（不含 <>），如 "账号定位" / "评审意见" / "配图策略"
  occurrence?: "first" | "last" | "all";  // 多个块时取哪个，默认 "last"
  includeTags?: boolean;    // 提取结果是否保留 <tag>...</tag>，默认 false（只取内部）
  join?: string;            // occurrence="all" 时多块拼接符，默认 ""
}

interface UnifiedResponse {
  content: string;          // 文本结果（流式时为增量累积）
  model: string;            // 实际使用的模型
  finish_reason: string;    // stop / length / content_filter
  usage: { prompt_tokens: number; completion_tokens: number };
  extracted?: string;       // 按 extractBlock 提取后的内容（未匹配则为 undefined）
  extractionMatched: boolean;  // 是否成功匹配到块
}
```

### 4.2 图像统一请求/响应

**ImageRequest**
```ts
interface ImageRequest {
  model: string;            // 逻辑图像模型，如 "dall-e-3" / "seedream-3.0" / "sd-xl"
  prompt: string;           // 配图提示词（由配图提示词 AI 产出）
  size?: string;            // 如 "1024x1024" / "16:9"
  count?: number;           // 生成张数，默认 1
  style?: string;           // 可选风格（即梦/Imagen 支持）
  // base_url / api_key 由网关注入
}

interface ImageResult {
  url?: string;             // 远程/中转站返回的可访问 URL
  base64?: string;          // 或本地回传的 base64（SD 本地）
  revised_prompt?: string;  // 模型回写的修正提示词（DALL·E 类）
}
```

### 4.3 统一异常（业务层按类型处理）
```ts
type GatewayError =
  | { kind: "AuthError"; provider: string; message: string }        // 401/403 Key 错
  | { kind: "RateLimitError"; provider: string; retryAfter?: number }
  | { kind: "TimeoutError"; provider: string }
  | { kind: "NetworkError"; provider: string; status?: number }
  | { kind: "ContentPolicyError"; provider: string }                // 内容被拦
  | { kind: "UnsupportedCapability"; capability: string; provider: string }
  | { kind: "ProviderConfigError"; message: string };               // base_url/key 缺失
```

### 4.4 核心方法签名
```ts
class ModelGateway {
  chat(req: UnifiedRequest): Promise<UnifiedResponse>;
  chatStream(req: UnifiedRequest, onToken: (t: string) => void): Promise<UnifiedResponse>;
  generateImage(req: ImageRequest): Promise<ImageResult[]>;
  listCapabilities(model: string): CapabilityFlags;
  listProviders(): ProviderMeta[];
}
```

---

## 5. 供应商适配（Provider Adapter）

### 5.1 Provider 配置结构（存本地）
```ts
interface ProviderConfig {
  provider_id: string;          // 唯一
  display_name: string;         // 展示名
  modality: "text" | "image";   // 文本 or 图像
  protocol: "openai-compatible" | "anthropic" | "gemini"
          | "bytedance-image" | "sd" | "mj-proxy";
  base_url: string;             // 官方 / 中转站 / 图像网关地址
  api_key_ref: string;          // 指向 KeyStore 的引用，不存明文
  default_model: string;        // 该供应商默认逻辑模型
  is_relay?: boolean;           // 是否中转站（影响错误提示文案）
  capability_flags: CapabilityFlags;
  enabled: boolean;
}

interface CapabilityFlags {
  chat: boolean;
  vision: boolean;       // 多模态读图
  json_mode: boolean;    // 结构化输出
  streaming: boolean;
  image: boolean;        // 图像生成
}
```

### 5.2 预设文本供应商（开箱即填，base_url 预置，用户只填 Key）
| 供应商 | protocol | 预置 base_url | 接入方式 |
|--------|----------|---------------|---------|
| ChatGPT（OpenAI 官方） | openai-compatible | `https://api.openai.com/v1` | 原生 |
| Deepseek | openai-compatible | `https://api.deepseek.com/v1` | 原生 |
| Kimi（Moonshot） | openai-compatible | `https://api.moonshot.cn/v1` | 原生 |
| 智谱（GLM） | openai-compatible | `https://open.bigmodel.cn/api/paas/v4` | 原生 |
| 豆包（火山方舟 Ark） | openai-compatible | `https://ark.cn-beijing.volces.com/api/v3` | 原生 |
| **Claude（Anthropic）** | **anthropic** | `https://api.anthropic.com` | **原生直连（已纳入）** |
| **Gemini（Google）** | **gemini** | `https://generativelanguage.googleapis.com` | **原生直连（已纳入）** |
| 自定义中转站（New API / One API） | openai-compatible | 用户填 | 中转站聚合（Claude/Gemini 经此更省事） |

> **关键判断（已拍板）**：Claude / Gemini **原生直连与中转站两条路都做**。原生走各自协议适配器（Anthropic Messages API / Gemini generateContent）；中转站作为聚合兜底入口，背后同样覆盖全部 7 家。两条路共用同一 `chat()` 抽象，业务层无感。原生适配器是 P0 必做项（见第 14 节），不再延后。

### 5.3 预设图像供应商（统一进网关）
| 供应商 | protocol | 说明 |
|--------|----------|------|
| DALL·E（OpenAI） | openai-compatible（images） | `gpt-image` / `dall-e-3`，走 OpenAI base_url |
| Gemini Imagen | gemini | 原生 generateContent（image），走 Gemini base_url |
| 即梦（字节 Doubao-Seedream） | bytedance-image | 火山方舟图像端点 |
| Stable Diffusion | sd | 本地（Automatic1111 / ComfyUI 类 API）或云端 SD API |
| Midjourney | mj-proxy | 第三方代理（如 midjourney-proxy），无官方 API |

> 图像协议比 LLM 更碎，**统一进网关**后业务层（配图生成 AI）只需 `generateImage(prompt, opts)`，适配差异全部沉到图像适配器。Midjourney 无官方 API，列 P1（依赖第三方代理，稳定性自担）。

### 5.4 能力差异与协商策略
- 调用前：网关查目标模型的 `capability_flags`。
- 业务层请求了不支持的能力（如向不支持 `json_mode` 的模型要结构化）→ 抛 `UnsupportedCapability`，UI 层预先禁用该选项。
- **`json_mode` 回退**：若模型不支持原生 json_mode，网关自动降级为"prompt 约束 + 启发式解析"，并在响应元数据标注 `json_mode_simulated: true`，保证上层 XML 纯文本解析不崩。
- **图像能力标志**：`image` 为 false 的供应商在配图功能里被禁用。

---

## 6. API Key 安全存储（KeyStore）

**原则**：Key 永不明文落盘、不进日志、不随配置导出、不在内存以外残留。

**Windows 本地实现（二选一，按技术栈）**
- **方案 A（推荐，原生最安全）**：Windows Credential Manager（`wincred` / `CredWrite`），Key 由系统 DPAPI 加密，随当前 Windows 用户。
- **方案 B（跨框架兜底）**：使用 OS 提供的 DPAPI（`CryptProtectData`）加密 Key 写入本地文件（如 `AppData\Local\...\.keys`），进程内解密。

**规则**
- 配置导出/备份时，Key 字段留空或占位（`******`），明确提示"导出不含密钥，迁移需重新填写"。
- 日志中 Key 一律脱敏（只显示前缀 4 位 + `****`）。
- 文本与图像供应商的 Key 同机制管理（各自 `api_key_ref`）。

---

## 7. 配置管理

- 所有 `ProviderConfig`（不含 Key 明文）存本地（SQLite 的 `providers` 表）。
- 用户可 **增 / 删 / 改** 任意供应商，可为一个供应商配多个逻辑模型别名。
- 全局"当前默认 Provider（文本）/ 默认图像 Provider"概念，供各 AI 角色未指定时回退。
- UI 提供"新增供应商"表单：`模态 / base_url / Key / 默认模型 / 能力勾选`，预置项一键填充 base_url。

---

## 8. 错误处理 / 重试 / 超时 / 降级

| 维度 | 策略 |
|------|------|
| **超时** | 固定 `request_timeout = 60s`（按建议值，可配）；图像生成放宽至 180s。 |
| **重试** | 仅对 `RateLimitError` / `NetworkError`(5xx) / `TimeoutError` 指数退避重试，最多 3 次（含初始）。4xx（含 401/403/404）**不重试**，直接报错。 |
| **降级 Provider** | 可选：用户可为某逻辑模型配置"备用 Provider"；主失败后自动切备用（需显式开启，避免意外成本）。 |
| **失败上报** | 所有错误归一为 `UnifiedError`，业务层按 `kind` 决定提示（如 AuthError→提示检查 Key，RateLimit→提示稍后重试）。 |
| **内容被拦** | `ContentPolicyError` 返回拦截原因片段，引导用户调整 prompt，不静默。 |

---

## 9. 可观测性（本地，无成本表）

每次调用本地记录（不对外发送）：
```
{ ts, provider, model, modality: "text"|"image", is_stream,
  latency_ms, prompt_tokens?, completion_tokens?,
  success: bool, error_kind?: string }
```
- 供 **健康看板**：各 Provider 成功率、平均延迟、token 吞吐，帮助排障。
- **不维护成本单价表、不做花费估算**（用户已拍板）。token 仅记录用于诊断，不乘以单价。
- 日志文件本地滚动，默认不记录 message / 图像内容（隐私），可开关开启调试日志。

---

## 10. 与上层（XML 包裹纯文本）的关系 —— 必须讲清

```
业务层 AI 角色：
  1. 取本账号 <账号定位> 纯文本（来自账号模块）
  2. 拼 system + user 消息（内容是一段含 <账号定位>…</账号定位> 的纯文本）
  3. 调 gateway.chat({ model, messages, json_mode:false })
  4. 收到 content 纯文本 → 业务层自己解析/落库

配图生成 AI：
  1. 取配图提示词（来自配图提示词 AI）
  2. 调 gateway.generateImage({ model, prompt, size })
  3. 收到 ImageResult[]（url/base64）→ 业务层落库并插入排版

网关视角：
  messages 是普通字符串数组，<账号定位> 对它毫无意义；prompt 是图像描述字符串，网关不理解其语义。
```

**结论**：XML 包裹纯文本是**业务层 prompt 工程约定**，图像 prompt 是**业务层配图策略产物**——二者都与网关解耦。

#### 10.1 统一响应块提取（抗 CoT，对所有 XML 输出角色生效）

模型常在正式结果前输出思考/解释（Chain-of-Thought），直接解析 XML 会失败。网关在 `chat()` 返回里提供泛型块提取，各角色**统一调用**而非各自实现：

```
业务层 AI 角色：
  1. 调 gateway.chat({ model, messages, json_mode:false,
       extractBlock: { tag: "账号定位", occurrence: "last" } })
  2. 网关先取 content 原文 → 按 <账号定位>…</账号定位> 截取（取最后一块）→ 填入 extracted
  3. 业务层优先用 res.extracted（matched=true）解析；未匹配用 res.content 兜底人工核对
```

- `tag` 视作不透明定界符，网关不解析语义；`occurrence` 默认 `last`（多个块时取最终版）。
- 评审等多角色场景：每个角色传各自 `tag`（如 `评审意见`），规则可经 `occurrence` / `includeTags` 微调。
- 该能力对原生 Claude/Gemini 适配器同样生效（均返回文本后统一后处理）。

---

## 11. 成功指标（Success Metrics）

| 指标 | 类型 | 目标 |
|------|------|------|
| 新增一个兼容模型所需改动 | 健康（扩展性） | 仅配置，业务代码 0 改动 |
| 兼容模型可生成成功率（文本） | 领先 | 100%（7 家原生 + 中转站示例通过） |
| 兼容图像模型可生成成功率 | 领先 | 100%（DALL·E / Imagen / 即梦 + 至少 1 中转站示例通过） |
| Key 明文泄露面 | 健康（安全） | 0（无明文落盘/日志） |
| 平均调用延迟（P95，不含生成） | 健康 | < 网关开销 200ms（不含厂商 RT） |
| json_mode 不可用时的回退成功率 | 健康 | 100%（结构化解析不崩） |
| 响应块提取成功率（含 CoT 前缀） | 健康 | 100%（所有 XML 输出角色解析不崩） |

---

## 12. 风险与缓解

| 风险 | 影响 | 缓解 |
|------|------|------|
| 厂商协议/字段变动（如 base_url 改路径） | 适配失效 | 适配层隔离；OpenAI-compatible 为主，原生适配器按需更新 |
| 中转站稳定性 / 跑路 | 调用全挂 | 网关降级 + 多中转站可配；明确"中转站由用户自担" |
| Key 泄露 | 资损/安全风险 | 本地加密存储，不落明文日志，导出脱敏 |
| 国内模型 json_mode/function calling 支持不一 | 结构化不稳 | 统一回退 prompt 约束 + 解析，元数据标注 |
| 速率波动 / 限流 | 体验 | 超时、重试、降级 Provider |
| 图像模型协议碎、第三方代理不稳（MJ） | 配图失败 | 图像适配器隔离；MJ 列为 P1，主推 DALL·E/Imagen/即梦/SD |
| 原生 Claude/Gemini 维护成本 | 工程负担 | 已纳入 P0，适配层独立，影响面可控 |

---

## 13. 验收标准（Acceptance Criteria）

1. 给定 `base_url + key + model`，网关 `chat()` 能对任意 **OpenAI-compatible 厂商**（Deepseek / Kimi / 智谱 / 豆包 / ChatGPT）成功返回文本。
2. **Claude 原生直连**（`anthropic` 协议）与 **Gemini 原生直连**（`gemini` 协议）均能成功返回文本；经中转站亦可。
3. 新增一个供应商 = 仅填配置，业务层 `chat()` / `generateImage()` 代码 **0 改动** 即可使用。
4. Key 在本地存储中为加密态；日志/导出中 Key **不可见明文**。
5. 请求不支持的能力 → 返回 `UnsupportedCapability` 或自动回退成功解析，业务不崩。
6. `RateLimit/5xx/Timeout` 自动重试（≤3 次）；`4xx` 不重试直接报错。
7. `generateImage()` 对至少 2 类图像模型（如 DALL·E + 即梦）成功返回图片。
8. 每次调用本地记录用量与延迟（不发送外部，无成本字段）。

---

## 14. MVP 实施路径（最小可用版先做这些）

**P0（必须先有）**
1. 统一抽象 `UnifiedRequest/Response/ImageRequest/ImageResult/Error` + `chat()` + `generateImage()`。
2. **OpenAI-compatible 通用适配器**（文本核心，覆盖 5 家国内 + ChatGPT）。
3. **原生 Anthropic Adapter（Claude）** —— 已拍板，纳入 P0。
4. **原生 Gemini Adapter（Gemini）** —— 已拍板，纳入 P0。
5. **图像适配器 P0 子集**：OpenAI Image（DALL·E）+ 即梦（ByteDance），打通 `generateImage()`。
6. 预设供应商 base_url 一键填充 + 用户填 Key 的表单（文本 + 图像）。
7. **KeyStore**：Windows 本地加密存储（方案 A 或 B）。
8. **中转站接入**：`base_url` 可自定义 → 任意厂商经中转站跑通。
9. 基础能力协商（chat / streaming / json_mode / image 标志与回退）+ **统一响应块提取 `extractBlock`**（泛型按标签名截取，覆盖所有 XML 输出角色，抗 CoT）。
10. 固定超时（60s）+ 重试（≤3 次）。

**P1（增强，不阻塞 MVP 主线）**
11. Gemini Imagen 原生图像适配器 / SD 适配器（本地）。
12. Midjourney 第三方代理适配器（稳定性自担）。
13. 降级 Provider（备用供应商自动切换）。
14. vision 能力打通（配图生成结果回看等场景）。
15. 用量/健康本地看板（不含成本）。

---

## 15. 已拍板事项（原开放问题，本轮全部闭合）

1. **原生 vs 中转站**：两条路都做，Claude/Gemini 原生直连纳入 P0，中转站并行作为聚合兜底。
2. **图像模型是否进网关**：统一进网关，新增 `generateImage()` 抽象与图像适配器层（P0 含 DALL·E + 即梦）。
3. **超时/重试**：采用建议固定值（60s / 3 次）。
4. **成本单价表**：不做，移除花费估算；仅本地记录 token 用量与延迟供排障。
5. **统一响应块提取**：所有 XML 输出角色（账号定位/选题/框架/写文章/评审/配图…）共用网关层 `extractBlock` 泛型截取能力（抗 CoT），不再各自实现解析。

---

*下一步*：模型网关 P0 落地 → 账号定位 AI 联调 → 模块二：热点模块（PRD-热点模块.md）。
